desc(
	mode: "source"
	language: "general"
	title: "Huggingface Access Token"
	title_zh: "检测到 Huggingface Access Token",
	type: vuln
	level: low
	rule_id: "source-secrets-huggingface-access-token"
	risk: "hardcoded-credentials"
	desc: <<<DESC
Migrated from rule `huggingface-access-token` (upstream/generic/secrets/gitleaks/huggingface-access-token.yaml).
Original message: A gitleaks huggingface-access-token was detected which attempts to identify hard-coded credentials. It is not recommended to store credentials in source-code, as this risks secrets being leaked and used by either an internal or external malicious adversary. It is recommended to use environment variables to securely provide credentials or retrieve credentials from a secure vault or HSM (Hardware Security Module).

CWE: CWE-798: Use of Hard-coded Credentials
References:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
DESC
	solution: <<<SOLUTION
Remove the hardcoded token from source code; load it from an environment variable, a secrets manager, or a secure vault, and rotate the exposed credential.
SOLUTION
	alert_min: 1
	"file://pos.sample": <<<POS
token = "hf_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
POS
	"safefile://neg.sample": <<<NEG
token = "not-a-huggingface-token"
NEG
)

${*}.pattern_regex(/(?:^|[\\'"` >=:])(hf_[a-zA-Z]{34})(?:$|[\\'"` <])/) as $hit
alert $hit for {
	level: "low",
	title: "Huggingface Access Token",
	title_zh: "检测到 Huggingface Access Token",
	message: "Huggingface access token detected.",
}
