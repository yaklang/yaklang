desc(
	mode: "source"
	language: "general"
	title: "检测到未使用二进制模式读取"
	title_zh: "检测到未使用二进制模式读取"
	type: portability
	level: middle
	rule_id: "source-portability-prefer-read-in-binary-mode"
	risk: "code-style"
	desc: <<<DESC
### 规则说明
该规则用于检测 未使用二进制模式读取 相关的风险模式，属于 代码规范与兼容性 类问题。

### 风险类型
代码规范与兼容性（code-style）

### 影响
使用了已废弃、不兼容或不符合规范的模式，可能导致后续维护困难或运行环境差异。

### 检测方式
源码模式正则匹配，检测代码或配置中符合该风险模式的位置。
DESC
	solution: <<<SOLUTION
### 修复建议
改用推荐 API 或兼容写法，并按照项目规范统一代码风格。
SOLUTION
	alert_min: 1
	"file://pos.ml": <<<POS
open_in
POS
	"safefile://pos.ml": <<<NEG
In_channel.with_open_bin
NEG
)

${*.ml}.pattern_regex(/(?:open_in)/) as $p0
${*.mli}.pattern_regex(/(?:open_in)/) as $p1
$p0 + $p1 as $hit
alert $hit for {
	level: "middle",
	title: "检测到未使用二进制模式读取",
	title_zh: "检测到未使用二进制模式读取",
	message: "检测到未使用二进制模式读取",
}
