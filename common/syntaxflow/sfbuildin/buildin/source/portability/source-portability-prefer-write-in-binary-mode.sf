desc(
	mode: "source"
	language: "general"
	title: "Prefer Write In Binary Mode"
	title_zh: "Prefer Write In Binary Mode"
	type: portability
	level: middle
	rule_id: "source-portability-prefer-write-in-binary-mode"
	risk: "code-style"
	desc: <<<DESC
### 规则说明
'open_out' behaves differently on Windows and on Unix-like systems with respect to line endings. To get the same behavior everywhere, use 'open_out_bin' or 'open_out_gen [Open_binary]'. If you really want LF-to-CRLF translations to take place when running on Windows, use 'open_out_gen [Open_text]'.

### 风险类型
代码规范与兼容性（code-style）

### 影响
使用了已废弃、不兼容或不符合规范的模式，可能导致后续维护困难或运行环境差异。

### 检测方式
Source 模式正则匹配，检测代码或配置中符合该风险模式的位置。

### 补充说明
Note: source-mode regex approximation of a safe-literal pattern.
DESC
	solution: <<<SOLUTION
### 修复建议
改用推荐 API 或兼容写法，并按照项目规范统一代码风格。

推荐修复：
open_out_bin
SOLUTION
	alert_min: 1
	"file://pos.ml": <<<POS
open_out
POS
	"safefile://pos.ml": <<<NEG
Out_channel.with_open_bin
NEG
)

${*.ml}.pattern_regex(/(?:open_out)/) as $p0
${*.mli}.pattern_regex(/(?:open_out)/) as $p1
$p0 + $p1 as $hit
alert $hit for {
	level: "middle",
	title: "Prefer Write In Binary Mode",
	title_zh: "Prefer Write In Binary Mode",
	message: "Prefer Write In Binary Mode detected.",
}
