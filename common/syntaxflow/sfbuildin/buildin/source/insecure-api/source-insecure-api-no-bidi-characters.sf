desc(
	mode: "source"
	language: "general"
	title: "No Bidi Characters"
	title_zh: "检测到No Bidi Characters",
	type: vuln
	level: middle
	rule_id: "source-insecure-api-no-bidi-characters"
	risk: "unicode-direction-control"
	desc: <<<DESC
Migrated from Semgrep rule `no-bidi-characters` (/tmp/semgrep-rules/solidity/security/no-bidi-characters.yaml).
Original message: The code must not contain any of Unicode Direction Control Characters

CWE: CWE-837: Improper Enforcement of a Single, Unique Action
References:
- https://entethalliance.org/specs/ethtrust-sl/v1/#req-1-unicode-bdo
DESC
	solution: <<<SOLUTION
Review the flagged code and follow the framework's secure coding guidance: use safe APIs, escape output, and avoid hardcoded credentials.
SOLUTION
	alert_min: 1
	"file://pos.sol": <<<POS
‪

POS
	"safefile://neg.sample": <<<NEG
# safe sample
no secrets here
just regular code
NEG
)

${*.sol}.pattern_regex(/‪/) as $p0
${*.sol}.pattern_regex(/‫/) as $p1
${*.sol}.pattern_regex(/‭/) as $p2
${*.sol}.pattern_regex(/‮/) as $p3
${*.sol}.pattern_regex(/⁦/) as $p4
${*.sol}.pattern_regex(/⁧/) as $p5
${*.sol}.pattern_regex(/⁨/) as $p6
${*.sol}.pattern_regex(/‬/) as $p7
${*.sol}.pattern_regex(/⁩/) as $p8
$p0 + $p1 + $p2 + $p3 + $p4 + $p5 + $p6 + $p7 + $p8 as $hit
alert $hit for {
	level: "middle",
	title: "No Bidi Characters",
	title_zh: "检测到No Bidi Characters",
	message: "No Bidi Characters",
}
