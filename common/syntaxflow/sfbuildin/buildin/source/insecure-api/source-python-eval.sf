desc(
	mode: "source"
	language: "python"
	title: "Detect Python eval Usage"
	title_zh: "检测 Python eval 调用",
	type: vuln
	level: high
	rule_id: "source-python-eval"
	risk: "code-injection"
	desc: <<<DESC
### 规则说明
检测 Detect Python eval Usage 相关的风险模式。

### 风险类型
代码注入（code-injection）

### 影响
不可信输入被直接用于动态代码执行，可能导致任意代码执行。

### 检测方式
Source 模式正则匹配，检测代码或配置中符合该风险模式的位置。
DESC
	solution: <<<SOLUTION
### 修复建议
避免动态执行不可信代码，使用安全的解析和求值方式。
SOLUTION
	alert_min: 1
	"file://dyn.py": <<<POS
def run(user_input):
    return eval(user_input)
POS
	"safefile://dyn-safe.py": <<<NEG
import ast
def run(user_input):
    return ast.literal_eval(user_input)
NEG
)

${*.py}.pattern_regex(/(?m)(?<!literal_)(?<!ast\.)\beval\s*\(/) as $hit
alert $hit for {
	level: "high",
	title: "Python eval()",
	title_zh: "Python eval()",
	message: "Dangerous eval() call detected",
}
