desc(
	mode: "source"
	language: "general"
	title: "检测到 OCaml 临时文件使用"
	title_zh: "检测到 OCaml 临时文件使用"
	type: vuln
	level: middle
	rule_id: "source-insecure-api-ocamllint-tempfile"
	risk: "symlink-toctou"
	desc: <<<DESC
### 规则说明
该规则用于检测 OCaml 临时文件使用 相关的风险模式，属于 符号链接 TOCTOU 类问题。

### 风险类型
符号链接 TOCTOU（symlink-toctou）

### 影响
临时文件或路径在检查与使用之间可能被替换为符号链接，导致任意文件读写。

### 检测方式
源码模式正则匹配，检测代码或配置中符合该风险模式的位置。

### 参考
- CWE-367
DESC
	solution: <<<SOLUTION
### 修复建议
使用安全的临时文件 API，避免基于路径的检查后使用。
SOLUTION
	alert_min: 1
	"file://pos.ml": <<<POS
Filename.temp_file
POS
	"safefile://pos.ml": <<<NEG
Filename.get_temp_dirname
NEG
)

${*.ml}.pattern_regex(/(?:Filename\.temp_file)/) as $p0
${*.mli}.pattern_regex(/(?:Filename\.temp_file)/) as $p1
$p0 + $p1 as $hit
alert $hit for {
	level: "middle",
	title: "检测到 OCaml 临时文件使用",
	title_zh: "检测到 OCaml 临时文件使用",
	message: "检测到 OCaml 临时文件使用",
}
