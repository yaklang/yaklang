desc(
	mode: "source"
	language: "general"
	title: "检测到 std::env::current_exe 使用"
	title_zh: "检测到 std::env::current_exe 使用"
	type: vuln
	level: low
	rule_id: "source-insecure-api-current-exe"
	risk: "information-exposure"
	desc: <<<DESC
### 规则说明
该规则用于检测 std::env::current_exe 使用 相关的风险模式，属于 敏感信息泄露 类问题。

### 风险类型
敏感信息泄露（information-exposure）

### 影响
应用可能暴露内部路径、环境信息或敏感数据，攻击者可借此收集攻击面。

### 检测方式
源码模式正则匹配，检测代码或配置中符合该风险模式的位置。

### 参考
- CWE-807
DESC
	solution: <<<SOLUTION
### 修复建议
限制敏感信息的输出范围，关闭不必要的调试接口，并对暴露内容进行脱敏。
SOLUTION
	alert_min: 1
	"file://pos.rs": <<<POS
std::env::current_exe()
POS
	"safefile://pos.rs": <<<NEG
std::env::current_dir()
NEG
)

${*.rs}.pattern_regex(/(?:std::env::current_exe\(\))/) as $hit
alert $hit for {
	level: "low",
	title: "检测到 std::env::current_exe 使用",
	title_zh: "检测到 std::env::current_exe 使用",
	message: "检测到 std::env::current_exe 使用",
}
