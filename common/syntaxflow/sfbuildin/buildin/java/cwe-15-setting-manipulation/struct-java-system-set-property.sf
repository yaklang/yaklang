desc(
	mode: "struct"
	language: "java"
	title: "结构扫描：Java System.setProperty 接受外部值"
	title_zh: "结构扫描：Java System.setProperty 接受外部值"
	type: vuln
	level: high
	rule_id: "struct-java-system-set-property"
	risk: "security-misconfiguration"
	cwe: "CWE-15"
	desc: <<<DESC
### 规则说明
System.setProperty 会改变 JVM 全局配置。值来自请求时可以改写安全相关系统属性。

### 风险类型
security-misconfiguration

### 影响
命中说明编译单元里出现了该调用，而不是注释或字符串。

### 检测方式
结构扫描匹配真实调用。
### 参考
- CWE-15
DESC
	solution: <<<SOLUTION
### 修复建议
不要用请求参数作为系统属性的值。只允许固定键和白名单值。
SOLUTION
	alert_min: 1
	"file://bad.java": <<<POS
class Bad { void c(String v) { System.setProperty("app.mode", v); } }

POS
	"safefile://ok.java": <<<NEG
class Ok { void c() { int n = 1; } }

NEG
)
setProperty as $call
setProperty(* as $arg) as $call
alert $call for {
	level: "high",
	title: "结构扫描：Java System.setProperty 接受外部值",
	title_zh: "结构扫描：Java System.setProperty 接受外部值",
	message: "结构扫描：Java System.setProperty 接受外部值",
	risk: "security-misconfiguration",
}
