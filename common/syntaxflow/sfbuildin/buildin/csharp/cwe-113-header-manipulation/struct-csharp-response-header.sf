desc(
	mode: "struct"
	language: "csharp"
	title: "结构扫描：C# 响应头写入外部值"
	title_zh: "结构扫描：C# 响应头写入外部值"
	type: vuln
	level: high
	rule_id: "struct-csharp-response-header"
	risk: "crlf-injection"
	cwe: "CWE-113"
	desc: <<<DESC
### 规则说明
Response.Headers.Add 或 AppendHeader 的值若含 CR/LF，可以注入额外响应头。

### 风险类型
crlf-injection

### 影响
命中说明编译单元里出现了该调用，而不是注释或字符串。

### 检测方式
结构扫描匹配真实调用。
### 参考
- CWE-113
DESC
	solution: <<<SOLUTION
### 修复建议
去掉值里的回车和换行，或使用框架提供的已编码头部 API。
SOLUTION
	alert_min: 1
	"file://bad.cs": <<<POS
class Bad { void h(Microsoft.AspNetCore.Http.HttpResponse resp, string v) { resp.Headers.Add("X-Name", v); } }

POS
	"safefile://ok.cs": <<<NEG
class Ok { void h() { int n = 1; } }

NEG
)
Add as $call
Add(* as $arg) as $call
alert $call for {
	level: "high",
	title: "结构扫描：C# 响应头写入外部值",
	title_zh: "结构扫描：C# 响应头写入外部值",
	message: "结构扫描：C# 响应头写入外部值",
	risk: "crlf-injection",
}
