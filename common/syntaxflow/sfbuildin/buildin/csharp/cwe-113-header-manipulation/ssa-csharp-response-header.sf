desc(
	mode: "ssa"
	language: "csharp"
	title: "数据流：C# 响应头写入外部值"
	title_zh: "数据流：C# 响应头写入外部值"
	type: vuln
	level: high
	rule_id: "ssa-csharp-response-header"
	risk: "crlf-injection"
	cwe: "CWE-113"
	desc: <<<DESC
### 规则说明
Response.Headers.Add 或 AppendHeader 的值若含 CR/LF，可以注入额外响应头。

### 风险类型
crlf-injection

### 影响
实参本身来自另一次调用时告警。常量字符串不报。

### 检测方式
SSA 上匹配调用，保留非常量且非 param 的实参。
### 参考
- CWE-113
DESC
	solution: <<<SOLUTION
### 修复建议
去掉值里的回车和换行，或使用框架提供的已编码头部 API。
SOLUTION
	alert_min: 1
	"file://bad.cs": <<<POS
class Bad { void h(Microsoft.AspNetCore.Http.HttpResponse resp, Microsoft.AspNetCore.Http.HttpRequest req) { resp.Headers.Add("X-Name", req.Get("n")); } }

POS
	"safefile://ok.cs": <<<NEG
class Ok { void h(Microsoft.AspNetCore.Http.HttpResponse resp) { resp.Headers.Add("X-Name", "fixed"); } }

NEG
)
Add(*?{opcode: call} as $dyn) as $call
alert $dyn for {
	level: "high",
	title: "数据流：C# 响应头写入外部值",
	title_zh: "数据流：C# 响应头写入外部值",
	message: "数据流：C# 响应头写入外部值",
	risk: "crlf-injection",
}
