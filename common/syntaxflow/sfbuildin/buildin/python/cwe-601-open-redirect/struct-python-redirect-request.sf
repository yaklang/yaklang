desc(
	mode: "struct"
	language: "python"
	title: "结构扫描：Python redirect 使用请求数据"
	title_zh: "结构扫描：Python redirect 使用请求数据"
	type: vuln
	level: high
	rule_id: "struct-python-redirect-request"
	risk: "open-redirect"
	cwe: "CWE-601"
	desc: <<<DESC
### 规则说明
把请求参数传给 Flask redirect 或 Django HttpResponseRedirect，可以把用户带到任意站点。

### 风险类型
open-redirect

### 影响
命中说明编译单元里出现了该调用，而不是注释或字符串。

### 检测方式
结构扫描匹配真实调用。
### 参考
- CWE-601
DESC
	solution: <<<SOLUTION
### 修复建议
只允许站内相对路径，并用 url_has_allowed_host_and_scheme 校验主机。
SOLUTION
	alert_min: 1
	"file://bad.py": <<<POS
def bad(request):
    return redirect(request.args.get("next"))

POS
	"safefile://ok.py": <<<NEG
def ok():
    return "/home"

NEG
)
redirect as $call
redirect(* as $arg) as $call
alert $call for {
	level: "high",
	title: "结构扫描：Python redirect 使用请求数据",
	title_zh: "结构扫描：Python redirect 使用请求数据",
	message: "结构扫描：Python redirect 使用请求数据",
	risk: "open-redirect",
}
