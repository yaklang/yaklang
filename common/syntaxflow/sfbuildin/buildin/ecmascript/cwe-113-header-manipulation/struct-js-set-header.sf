desc(
	mode: "struct"
	language: "javascript"
	title: "结构扫描：JavaScript setHeader 写入外部值"
	title_zh: "结构扫描：JavaScript setHeader 写入外部值"
	type: vuln
	level: high
	rule_id: "struct-js-set-header"
	risk: "crlf-injection"
	cwe: "CWE-113"
	desc: <<<DESC
### 规则说明
res.setHeader 的值来自请求时，CR/LF 可以注入额外响应头。

### 风险类型
crlf-injection

### 影响
命中说明编译单元里出现了该调用，而不是注释或字符串。

### 检测方式
结构扫描匹配真实调用。
### 参考
- CWE-113
DESC
	solution: <<<SOLUTION
### 修复建议
去掉头部值中的换行。
SOLUTION
	alert_min: 1
	"file://bad.js": <<<POS
function bad(res, v) { res.setHeader("X-Name", v); }

POS
	"safefile://ok.js": <<<NEG
function ok() { return 1; }

NEG
)
setHeader as $call
setHeader(* as $arg) as $call
alert $call for {
	level: "high",
	title: "结构扫描：JavaScript setHeader 写入外部值",
	title_zh: "结构扫描：JavaScript setHeader 写入外部值",
	message: "结构扫描：JavaScript setHeader 写入外部值",
	risk: "crlf-injection",
}
