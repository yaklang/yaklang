## 代码安全审计任务

你的任务是对指定代码项目执行全面安全审计。

{{ if .Forge.UserParams }}
## 审计参数
{{ .Forge.UserParams }}
{{ end }}

## 执行指令

**必须立即调用 `load_capability` 进入 `code_security_audit` 专注模式（focus mode）来完成审计。**

```
{"@action": "load_capability", "identifier": "code_security_audit", "human_readable_thought": "进入 code_security_audit 专注模式，执行四阶段安全审计流水线"}
```

**重要约束**：
- **不要**调用 `require_ai_blueprint` 或 `require_forge`，直接使用 `load_capability code_security_audit`
- `code_security_audit` 是一个 **focus mode ReAct loop**，不是 forge，必须通过 `load_capability` 调用
- `project_path` 参数已通过配置注入，`code_security_audit` loop 会自动读取
