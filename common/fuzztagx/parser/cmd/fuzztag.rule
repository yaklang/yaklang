# 词法大写字母开头，允许使用正则
# 语法小写字母开头，产生式右侧可以使用 /* /+ 标志匹配次数：至少0次 至少1次
TagLeft = {{
TagRight = }}
MethodParamLeft = (
MethodParamRight = )
IdentifierStart = [A-Z_]
IdentifierOther = [a-zA-Z_]
Param = .* | Method | FuzzTag
Empty = [ |\n]+

identifier = IdentifierStart IdentifierOther
method = identifier MethodParamLeft Param MethodParamRight
methodWithEmpty = Empty/* method Empty/*
fuzzTag = TagLeft methodWithEmpty/+ TagRight