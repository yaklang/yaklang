package ldap

import (
	"testing"
)

func TestNewLdapServer(t *testing.T) {
	//server := ldapserver.NewLdapServer()
	//server := ldapserver.NewLdapServerWithPort(9092, 9091)
	//server.SetPayload(yso.GetTomcatEcho("id"))
	//ctx, cancel := context.WithCancel(context.Background())
	//defer cancel()
	//go server.Run(ctx)
	//println(server.GetUrl())
	//utils.WaitConnect(server.GetAddr(), 5)
	//time.Sleep(time.Duration(5000000) * time.Second)
}
