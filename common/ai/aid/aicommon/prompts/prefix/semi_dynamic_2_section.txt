{{ if .ExecutionPolicy }}<|EXECUTION_POLICY|>
## 执行策略

以下是当前会话的顶层运行策略，决定你如何选择动作、是否拆分并行子 Agent、以及何时收尾。这些约束优先级高于你的默认习惯，必须严格遵守；仅在策略未覆盖时才回退到常规判断。

{{ .ExecutionPolicy }}
<|EXECUTION_POLICY_END|>{{ end }}{{ if .TaskInstruction }}

<|PERSISTENT|>
{{ .TaskInstruction }}
<|PERSISTENT_END|>{{ end }}{{ if .OutputExample }}

<|OUTPUT_EXAMPLE|>
{{ .OutputExample }}
<|OUTPUT_EXAMPLE_END|>{{ end }}{{ if .AutoLoadedSkills }}
{{ .AutoLoadedSkills }}{{ end }}{{ if .Schema }}

当前任务循环中可以使用的Action和<|TAG...|>，如下 ：

<|SCHEMA|>
```jsonschema
{{ .Schema }}
```
<|SCHEMA|>{{ end }}
