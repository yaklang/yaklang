{{ .ContextProvider.Progress }}
--- 当前任务 ---
{{ .ContextProvider.CurrentTask.GetUserInput }}

任务次数执行信息：
- 调用了{{ .ContextProvider.CurrentTask.ToolCallCount }} 次工具。
- 决策继续执行了{{ .ContextProvider.CurrentTask.TaskContinueCount}} 次。（系统限制为{{ .ContextProvider.TaskMaxContinue}}次）

{{ if .ContextProvider.CurrentTask.SingleLineStatusSummary }}任务状态：{{ .ContextProvider.CurrentTask.SingleLineStatusSummary }}{{end}}

{{ if .ContextProvider.SharedEvidenceContext }}
--- 共享执行证据 ---
{{ .ContextProvider.SharedEvidenceContext }}
{{end}}
--- 先决条件检查 ---
在开始执行当前任务前，请先判断：
- 优先阅读当前任务输入中的 FACTS 或 EVIDENCE 区块，以及上面的共享执行证据
- 如果 evidence 中提到文件、路径、接口、参数或配置，使用 find_file、grep、read_file 工具先确认这些内容确实存在于工作目录中
- 不要依赖前序任务 summary 或记忆假设，始终以工具读取和 EVIDENCE 为准