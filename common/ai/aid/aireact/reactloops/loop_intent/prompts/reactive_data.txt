{{ if eq .Language "zh" }}LANGUAGE: 请使用中文进行意图分析和输出。{{ else }}LANGUAGE: Please perform intent analysis and output in English.{{ end }}

## 当前意图识别状态 / Intent Recognition State

### 用户查询 / User Query
{{.UserQuery}}

{{if .CatalogMatchedIdentifiers}}
### 能力目录预匹配结果 / Pre-matched Identifiers from Capability Catalog
{{ if eq .Language "zh" }}以下 identifier 已通过系统能力目录验证，确认存在：{{ else }}The following identifiers have been verified to exist in the system:{{ end }}
{{.CatalogMatchedIdentifiers}}
{{end}}

### 已获取的搜索结果 / Search Results
{{if .SearchResults}}
{{.SearchResults}}
{{else}}
{{ if eq .Language "zh" }}尚未执行搜索。请先使用 `query_capabilities` 搜索相关能力。{{ else }}No searches performed yet. Use `query_capabilities` to search for relevant capabilities.{{ end }}
{{end}}

### 意图分析进度 / Intent Analysis Progress
{{if .IntentAnalysis}}
{{.IntentAnalysis}}
{{else}}
{{ if eq .Language "zh" }}意图分析尚未完成。{{ else }}Intent analysis has not been performed yet.{{ end }}
{{end}}

## 操作指引 / Instructions

{{ if eq .Language "zh" }}根据用户的查询和已收集的搜索结果：
1. 如果尚未搜索，使用 `query_capabilities` 搜索相关的工具和蓝图。搜索关键词需同时包含中文和英文。对于综合性任务，可能需要多次搜索覆盖不同子任务。
2. 如果搜索已完成且信息充足，使用 `finalize_enrichment` 生成最终的上下文增强。
3. 始终聚焦于用户的核心意图，找到能直接解决其需求的能力。
4. **只推荐搜索结果中实际出现的能力名称**，不要编造不存在的 identifier。
5. 如果用户输入直接包含了某个 identifier，优先确认并推荐该能力。
{{ else }}Based on the user's query and search results gathered so far:
1. If no searches have been done, use `query_capabilities` to find relevant tools and forges. Include both Chinese and English keywords. For composite tasks, multiple searches may be needed.
2. If searches are complete, use `finalize_enrichment` to produce the final context enrichment.
3. Focus on finding capabilities that directly address the user's stated intent.
4. **Only recommend capabilities that actually appear in search results**. Do NOT fabricate identifiers.
5. If the user's input directly contains an identifier name, prioritize and recommend that capability.
{{ end }}
