// test_multi_file_analysis.yak
// 测试 loop_report_generating 的多文件分析功能
// 使用 actions: read_reference_file（多个文件）, write_section, modify_section
//
// 测试断言：
// 1. 必须成功获取报告文件路径
// 2. 报告内容长度必须大于 100 字节
// 3. 报告应包含多个章节相关内容

var reportFilename = ""
var reportContent = ""
var executionError = nil

// 测试任务：分析多个文件并生成综合报告
testPrompt = `请帮我分析 Yaklang 项目的文档结构，需要阅读以下文件：

1. /Users/v1ll4n/Projects/yaklang/README.md - 中文版说明
2. /Users/v1ll4n/Projects/yaklang/README_EN.md - 英文版说明
3. /Users/v1ll4n/Projects/yaklang/LICENSE.md - 许可证文件

任务要求：
1. 使用 read_reference_file 分别阅读上述三个文件
2. 生成一份对比分析报告，包含以下章节：
   - 项目概述（基于 README）
   - 国际化支持情况（对比中英文文档）
   - 开源许可类型分析

报告长度：200-300 字`

log.Info("========================================")
log.Info("Testing loop_report_generating - Multi-File Analysis")
log.Info("========================================")

err = aim.InvokeReAct(
    testPrompt,
    aim.focus("report_generating"),
    aim.timeout(360),
    aim.maxIteration(20),
    aim.onEvent(func(react, event) {
        eventType = string(event.Type)
        if eventType == "filesystem_pin_filename" {
            contentStr = string(event.Content)
            try {
                jsonData = json.loads(contentStr)
                if jsonData != nil && jsonData["path"] != nil {
                    reportFilename = jsonData["path"]
                }
            } catch e {
                // ignore
            }
        }
    }),
    aim.onFinished(func(react) {
        if reportFilename != "" {
            content, readErr = file.ReadFile(reportFilename)
            if readErr == nil {
                reportContent = string(content)
            }
        }
    }),
)

if err != nil {
    executionError = err
}

// ========================================
// 断言测试
// ========================================

log.Info("========================================")
log.Info("RUNNING ASSERTIONS")
log.Info("========================================")

// 断言1：执行不应该失败
assert executionError == nil, sprintf("ReAct execution failed: %v", executionError)

// 断言2：必须获取到报告文件路径
assert reportFilename != "", "Failed to capture report filename from events"

// 断言3：报告文件必须存在
assert file.IsExisted(reportFilename), sprintf("Report file does not exist: %s", reportFilename)

// 断言4：报告内容长度必须大于 100 字节
contentLen = len(reportContent)
assert contentLen > 100, sprintf("Report content too short. Expected > 100 bytes, got %d bytes", contentLen)

// 断言5：报告应该包含项目概述或 README 相关内容
hasProjectContent = str.Contains(reportContent, "项目") || str.Contains(reportContent, "Yaklang") || str.Contains(reportContent, "概述")
assert hasProjectContent, sprintf("Report should mention project overview. Content: %s", reportContent[:min(len(reportContent), 300)])

log.Info("========================================")
log.Info("ALL ASSERTIONS PASSED!")
log.Info("========================================")
log.Info("Report Filename: %s", reportFilename)
log.Info("Report Content Length: %d bytes", contentLen)
log.Info("Report Preview: %s", reportContent[:min(len(reportContent), 500)])
