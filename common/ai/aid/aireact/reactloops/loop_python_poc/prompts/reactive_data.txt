## Python Environment Status

{{ if .EnvChecked }}
{{ if .PythonAvailable }}
Python available: `{{ .PythonCommand }}` ({{ .PythonVersion }})
{{ else }}
WARNING: Python not available - syntax check will be skipped, code will include warning comments
{{ end }}
{{ else }}
ENVIRONMENT NOT CHECKED: You MUST first use the bash tool to check Python environment, then call `check_python_env` to report the result!

### Execute the following steps immediately:

1. Use bash tool to check Python:
```json
{"@action": "require_tool", "tool_require_payload": "bash", "human_readable_thought": "I need to check if Python is available on this system"}
```

2. Run command: `python3 --version` or `python --version`

3. Call `check_python_env` with the result
{{ end }}

---

{{ if .Code }}## Current POC Code

File path: `{{ .Filename }}`

**IMPORTANT: The code shown below includes line numbers for your reference when locating and understanding code structure. But when you use 'write_python_poc' or 'modify_python_poc' to generate new code, the generated code must NEVER include these line number prefixes! Only generate clean Python code!**

<|CURRENT_PYTHON_POC_CODE_{{ .Nonce }}|>
{{ .CurrentCodeWithLineNumber }}
<|CURRENT_PYTHON_POC_CODE_END_{{ .Nonce }}|>

### Next Steps

{{ if .PythonAvailable }}
Code saved. You need to:

1. **Check feedback**: If there are syntax errors in the feedback below, use `modify_python_poc` to fix them
2. **Verify syntax**: When no more syntax errors, call `verify_syntax` to confirm
3. **Complete task**: Use `directly_answer` to finish

NOTE: You cannot complete the task until `verify_syntax` is called.
{{ else }}
Code saved. Python environment is not available, syntax check was skipped.

You can:
- Continue using `modify_python_poc` to modify code
- Use `directly_answer` to complete the task
{{ end }}

{{ else }}## No POC Code Yet

{{ if .Filename }}File path: `{{ .Filename }}`{{ else }}**No file created yet**{{ end }}

{{ if .EnvChecked }}
### Generate POC Code

Use `write_python_poc` to create code:

```json
{"@action": "write_python_poc", "human_readable_thought": "Creating Python POC"}
```

<|GEN_PYTHON_POC_{{ .Nonce }}|>
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import requests
# Your code...
<|GEN_PYTHON_POC_END_{{ .Nonce }}|>
{{ else }}
### You MUST check the Python environment first!

Before generating code, please:

1. Use bash tool to check Python
2. Call `check_python_env` to report result
{{ end }}
{{ end }}

{{ if .FeedbackMessages }}
## Feedback

<|FEEDBACK_{{ .Nonce }}|>
{{ .FeedbackMessages }}
<|FEEDBACK_END_{{ .Nonce }}|>

### Handle Feedback

Take appropriate action based on the feedback above. If there are syntax errors, fix them with `modify_python_poc`.

{{ end }}
