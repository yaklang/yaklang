## SyntaxFlow 规则输出格式示例

* 使用 `write_rule` 创建新规则
* 使用 `modify_rule` 修改已有规则（指定 modify_start_line 和 modify_end_line）
* 每次生成或修改后，系统会进行 SyntaxFlow 编译验证

### 正确示例: write_rule

```json
{"@action": "write_rule", "human_readable_thought": "创建漏洞检测规则"}
```

<|GEN_RULE_{{ .Nonce }}|>
rule("test-rule")
desc(
	title: "Test"
	type: audit
	level: info
)
<|GEN_RULE_END_{{ .Nonce }}|>
