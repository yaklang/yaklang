## 当前 SyntaxFlow 规则

{{ if .SfHasCodeSample }}
**⚠️ 【有正例（file://、UNSAFE）】必须完成**：用户提供的漏洞样例即为正例。在 directly_answer 或回答任务完成之前，**必须调用** `check-syntaxflow-syntax` 并传入 path、sample_code=sf_sample_code、filename=sf_sample_filename、language=sf_sample_language 进行正例自检，得到 matched=true。严禁跳过此步骤。
{{ end }}

{{ if .Code }}
{{ if .SfFilename }}
**规则文件路径**（用于 check-syntaxflow-syntax 的 path 参数）: `{{ .SfFilename }}`
{{ end }}
<|CURRENT_SF_RULE_{{ .Nonce }}|>
{{ .CurrentCodeWithLineNumber }}
<|CURRENT_SF_RULE_END_{{ .Nonce }}|>

{{ if .FeedbackMessages }}

## 反馈/错误信息

{{ .FeedbackMessages }}

**⚠️ 【必须验证】修改后请立即调用 `check-syntaxflow-syntax` 验证**（传入 path 或 syntaxflow-code）{{ if .SfHasCodeSample }}，并传入 sample_code、filename、language 做正例自检{{ end }}。禁止在未调用验证工具的情况下再次 modify_rule。验证通过或得到 matched=true 后方可 directly_answer。
{{ if .IterationHigh }}

**⚠️ 已迭代 {{ .IterationCount }} 次**：禁止继续叠加条件或重写复杂逻辑。必须**回归 initial_rule_samples** 中与漏洞类型最接近的规则（如 SSTI→golang-template-ssti.sf），复制其规则体结构，仅调整方法名以适配样例，或先用最小模式 `.dangerousMethod(* #-> as $x); alert $x` 验证 sink 能否匹配。
{{ end }}

**若反馈涉及 desc、heredoc 或元数据语法错误**：请根据任务开始时已提供的规则样例及 golang-reflected-xss-gin-context.sf 确认正确结构。heredoc 结束符（TEXT、DESC 等）必须**行首无空格**。禁止盲目继续修改。

**若 verify 返回 matched=false**：优先查看 **diagnostic_hint**。理解变量依赖：$param 依赖 $context，$context 依赖 $gin；从下往上分析（$sink:0 因 $source:0，$source:0 因 $context:0，根因多为链首）。**标注 [未定义] 的变量**（如 $gin）不应存在，include 必须带 as $var。**若链首为 0**：拆分复合模式逐段验证。**若 include 相关变量为 0**：读取 awesome-rule 对应 lib 查看内部模式。迭代多次未通过时：**回归 initial_rule_samples**，用最小模式重试。
{{ else }}
**下一步**：必须调用 `check-syntaxflow-syntax` 验证{{ if .SfFilename }}，传入 path=`{{ .SfFilename }}`{{ else }}（可传 syntaxflow-code 为上方规则内容）{{ end }}{{ if .SfHasCodeSample }}，并传入 sample_code=sf_sample_code、filename=sf_sample_filename、language=sf_sample_language 进行正例（file://、UNSAFE）自检，matched=true 后{{ end }}方可 directly_answer。

**directly_answer 时**：**禁止输出任何规则代码**（禁止 ```syntaxflow、flow(、pattern、source=、match: 等）。系统将自动从 `{{ .SfFilename }}` 读取规则文件并展示。你只需输出 1-3 句话的简要总结（本规则检测目标、数据流要点），不要输出任何规则正文。
{{ end }}
{{ else }}
等待生成规则。使用 write_rule 创建新规则。
{{ end }}
