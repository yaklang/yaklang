任务说明:
请搜索与 CSRF 检测相关的 Yakit 安全插件，然后对 http://example.com 执行该插件进行安全分析。

期望行为:
1. 通过 search_capabilities 搜索，直接在 "Matched Yakit Plugins" 中找到 CSRF 相关的 MITM 插件（如 "CSRF 表单保护与 CORS 配置不当检测"）。
2. 直接加载并调用该 Yakit MITM 插件（而非通过 query_plugin_by_fp 或 call_yak_plugin 间接调用）。
3. 调用时传入 url 参数 http://example.com，插件分析 HTTP 流量。

通过标准:
- search_capabilities 直接返回 Yakit Plugins 列表，包含带 [MITM] 标签的 CSRF 检测插件。
- 插件被直接加载为 AI Tool 并执行（路径: search_capabilities → load CSRF plugin → call with url）。
- 不应出现 query_plugin_by_fp 或 call_yak_plugin 等中间工具调用。
- 流程中展示了工具的二次披露信息（Usage）。
