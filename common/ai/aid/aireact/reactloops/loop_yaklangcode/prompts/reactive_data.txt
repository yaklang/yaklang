{{ if or .WorkspacePath .EditorFilePath }}## 当前编辑器上下文
{{ if .WorkspacePath }}- 工作区目录：`{{ .WorkspacePath }}`
{{ end }}{{ if .EditorFilePath }}- 当前打开文件：`{{ .EditorFilePath }}`
{{ end }}优先在上述路径范围内读写 Yaklang 脚本；不要臆造其他文件路径。

{{ end }}{{ if .EditorFilePathWithoutCode }}## 编辑器路径与代码缓冲不一致
上下文记录了打开文件 `{{ .EditorFilePath }}`，但本次任务内存中尚未加载任何代码（full_code 为空）。
- 要**修改该文件**：用 `modify_code`（不要用 `write_code` 覆盖）；需要内容时依赖附件选区或磁盘文件，别臆造。
- 要**新建独立脚本**（create 模式）：忽略"打开文件"提示，用 `write_code`，并在 JSON 后必须附 `<|GEN_CODE_{{ .Nonce }}|>...<|GEN_CODE_END_{{ .Nonce }}|>`。

{{ end }}{{ if not .AIKBAvailable }}## AIKB 不可用
代码样例库未安装。仅用 `yakdoc_*` 查 API 签名；禁止臆造样例或 API。

{{ end }}{{ if .PinnedDSL }}## Yaklang DSL 硬规则（Init 锁定）
{{ .PinnedDSL }}

{{ end }}{{ if .PinnedAPIs }}## 已锁定接口签名（核心库 {{ .PinnedLibraries }}）
Init 从 YakDocument 抽取的权威 Decl。写码/修错优先采信；未列出的用 `yakdoc_function_details`。

{{ .PinnedAPIs }}

{{ end }}{{ if .HasInitialSamples }}## 预检索代码样例（Init 已完成）
语法通过时禁止对相同 pattern/question 重复 grep/semantic。`yak_lint_ok=false` 或 `yak_run_ok=false` 时可再检。

{{ .InitialCodeSamples }}
{{ if .InitSearchManifest }}已覆盖：{{ .InitSearchManifest }}
{{ end }}
{{ end }}{{ if .ModuleOverviewReady }}## 模块速览可用
不确定库时用 `yakdoc_module_overview(library="...")`。

{{ end }}{{ if .Code }}## 当前代码（行号仅供定位，生成代码时绝不能带行号）
<|CURRENT_CODE_{{ .Nonce }}|>
{{ .CurrentCodeWithLineNumber }}
<|CURRENT_CODE_END_{{ .Nonce }}|>

调试可用 `desc(instance)` / `log.Info(...)`。修改用 `modify_code` + Cursor Patch（`*** Begin Patch`）。Patch 的 context / `-` 行必须从上方 CURRENT_CODE 逐字符复制。
{{ else }}## 当前没有代码（create 模式）
新建用 `@action: write_code`。JSON 后必须紧跟完整代码块：
```
{"@action": "write_code", "human_readable_thought": "..."}
<|GEN_CODE_{{ .Nonce }}|>
...你的 Yaklang 代码...
<|GEN_CODE_END_{{ .Nonce }}|>
```
禁止只发 JSON；禁止 markdown ```yak。只想先查文档就改用查询动作。{{ end }}
{{ if .NeedsLintResearch }}
## 【强制】lint 修复门禁（modify_code 已隐藏）
先 `grep_yaklang_samples` / `semantic_search_*` / `yakdoc_*`，成功检索后才开放 `modify_code`。

{{ end }}{{ if .FeedbackMessages }}
## 本轮编译反馈（必须优先处理）
<|ERR/LINT_WARNING_{{ .Nonce }}|>
{{ .FeedbackMessages }}
<|ERR/LINT_WARNING_END_{{ .Nonce }}|>

1. API 不存在→`yakdoc_*`；SyntaxError/undefined→`grep_yaklang_samples`；参数错→`yakdoc_function_details`。
2. 检索后一次 `modify_code`；lint 失败允许重复 Init pattern。
{{ end }}{{ if .NeedsSelfTestBlock }}## 缺少 YAK_MAIN 自测块（语法已通过，系统无法自动运行验证）
脚本类型：`{{ .ScriptKind }}`。用 `modify_code` 末尾追加 `func runSelfTest(){...}` 与 `if YAK_MAIN { runSelfTest() }`（禁止 write_code）。

{{ if .SelfTestHint }}{{ .SelfTestHint }}
{{ end }}{{ end }}{{ if .RunFeedback }}
## 本轮运行反馈（YAK_MAIN 自测）
<|RUNTIME_ERR_{{ .Nonce }}|>
{{ .RunFeedback }}
<|RUNTIME_ERR_END_{{ .Nonce }}|>

先 `grep`/`yakdoc_*`（如需）再 `modify_code`；禁止 `write_code`。常见：assert、mock 报文 `\r\n`、map comma-ok、未定义 `runSelfTest`、对 `[]byte` 误调方法。
{{ end }}
