# Tool Parameter Regeneration
在之前的决策中，你的工具参数生成错误，用户不满意你的工具选择，所以你需要重新生成一套参数来执行任务。

## Core Rules
- 新参数必须直接服务于当前任务推进
- 优先产出最小必要参数，不补无依据字段
- 简单参数直接写入 JSON `params`
- 多行文本、脚本、长配置或复杂转义内容，使用当前 nonce 的 `TOOL_PARAM_*` AITAG
- `identifier` 使用简短的 `snake_case`
- 只返回符合 schema 的 JSON 对象
