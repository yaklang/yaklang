{{ if .AutoContext }}{{ .AutoContext }}{{ end }}

{{ if .EnhanceData }}<|ENHANCE_DATA_{{ .Nonce }}|>
# Knowledge Enhance 帮助判断的增强信息
{{- range .EnhanceData }}
{{ . }}

{{- end }}
<|ENHANCE_DATA_END_{{ .Nonce }}|>
{{ end }}

<|USER_ORIGINAL_QUERY_{{ .Nonce }}|>
{{ .UserQuery }}
<|USER_ORIGINAL_QUERY_END_{{ .Nonce }}|>

<|INPUT_{{ .Nonce }}|>{{ if .IsToolCall }}
# Tool Execution Context
Tool executed: {{ .Payload }}{{ else }}
User Raw INPUT/ReAction Thought/Observation:
{{ .Payload }}
{{ end }}<|INPUT_END_{{ .Nonce }}|>

{{ if .TodoSnapshot }}
# TODO:
<|TODO_SNAPSHOT_{{ .Nonce }}|>
{{ .TodoSnapshot }}
<|TODO_SNAPSHOT_END_{{ .Nonce }}|>
{{ end }}{{ if .HasRepeatedExecutionPath }}
## 执行路径检查
**当前子任务已多次经过相似执行路径。请重点检查：**
1. 当前子任务是否已有能直接支持完成的证据；阴性结论是否来自能区分假设真假的实验？
2. Agent 是否漂移到其它子任务；漂移只用于纠偏，不得单独作为当前任务完成证据。
3. 不要把“漏洞未成功利用”直接等同于“漏洞不存在”；单次失败、扫描未命中或无明显报错时应继续改进对照、观察通道或假设粒度。
{{ end }}
