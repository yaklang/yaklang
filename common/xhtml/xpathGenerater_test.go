package xhtml

import (
	"testing"
)

func TestGenerateXPath(t *testing.T) {
	//root, _ := htmlquery.Parse(strings.NewReader(testBody))
	//tr := htmlquery.Find(root, FindNodeFromHtml(testBody, "*hacker*")[0])
	//for _, t := range tr {
	//	println(t.Data)
	//}
}
