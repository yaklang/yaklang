// Package codeaudit provides static security audit primitives for code projects.
//
// It implements project probing, configuration file auditing, hardcoded secret
// scanning, and dependency SCA wrapping. The engine is language-agnostic;
// language-specific signals and rules live in the catalog/ subpackage.
package codeaudit
