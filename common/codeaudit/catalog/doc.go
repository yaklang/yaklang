// Package catalog holds language-specific signals and rules for codeaudit.
//
// It is separated from the engine to allow multi-language support.
// Each language has its own file (e.g. java.go, java_config.go).
package catalog
