# 本次工作范围与交接顺序

## 2026-09-07 本轮验收更新

本轮继续在原 PR #5013 上交付离线完整解析性能与明确消息范围内的 P0 验收，保持不合并。历史记录中 LDAP、MySQL、PostgreSQL、SMB3 的 P0 状态冲突，现由 [可执行消息范围](P0_SCOPE.md) 解决：补足消息实现后逐项执行正负例、字段、边界与原始样本检查；四项能力目录仍为 `partial`，不以修改标签或豁免代替测试。历史入口、两代捕获和十四项延期保持原合同。

`go test ./common/bin-parser/... -count=1 -timeout=5m` 已包含 P0 全部通过。性能基线改为用户指定的 `5b6cbe1f8`，沿用每批 11 条消息、1,421 B、完整字段及元数据 JSON 的原公开调用链。原始源码、各阶段补丁、测试程序、manifest 摘要、正式测量及失败实验见 [本轮报告](reports/hotpath-20260907/README.md)。实时带宽、客户端和 aid 能力不属于本轮完成项。以下旧轮次结论保留为历史证据。

更新于 2026-09-07，依据用户最新指令。此说明优先于历史阶段记录中“实现全部有样本协议后再评估”的表述。

## 明确延期：不计入本次 goal 验收

- 外层 10 项：AnyDesk、DingTalk、DoH、DoQ、DoT、HTTP/3、IMAPS、SMTPS、T.38、WeChat/MicroMsg。
- 识别 4 项：爱奇艺 P2P、腾讯游戏、网易游戏、米哈游/HoYoverse。

这 14 项留到下次。保留所有现有原始捕获、来源、摘要及已有合同，不删除材料、不升级支持状态、不将延期算作实现完成。已有封装/识别检查不等于其应用字段或会话语义已实现。

## 本次执行顺序

1. #5022、#5023 已先合并到 `wip/optimize-protocol-parse`，两代样本分别保留。
2. Cassandra：已完成本次样本范围内的字段解析与定向验证，具体边界见 corpus 说明；不声称完整协议支持。
3. Memcached：已补齐有样本的文本统计请求/完整响应，以及旧、新两代二进制 GET 请求；所有原始记录及错误边界的定向验证通过。
4. 随后立即转入性能评估、最大带宽评估与 aid/AI 分析能力评估；不再扩展其他协议实现。

## 评估口径

- 使用当前 PCAP 和明确的构建、硬件、入口、并发数与数据量，记录吞吐、包率、延迟、分配、内存和 GC；区分读取、封装解析、应用字段及结果导出成本。
- 离线解析字节率不是实时链路无丢包带宽上限。最大持续带宽还受包长、协议分布、重组、流状态、队列、输出和客户端渲染约束；未实测的能力明确列为未知。
- aid/AI 评估区分已有代码连接、可用结构化证据、分析正确性/可追溯性，以及尚未集成或实测的能力；不把每包调用模型作为默认方案，不凭推断宣称客户端流畅。
- 实现阶段继续使用定向测试；性能不能代替正确性验证。是否执行一次冻结后的全量，应依据评估风险与交付需要决定，而不是每次修改都重复全量。
- 本次不新增依赖。优化基于测量热点；已有正则组件的局部收益不能外推为全库吞吐。

进度：Cassandra、Memcached 样本范围的实现与联合定向验证均已通过。首轮性能、带宽和 AI 能力评估已形成 [可复现报告](PERFORMANCE_ASSESSMENT.md)：包含 1/4/8/10 worker、复杂列表、延迟、分配、RSS、热点与现有 aid/客户端连接核验。552 份捕获、58,533 条记录的完整性与封装检查重新通过；未将它们等同于完整应用语义或整个库的全量回归。实时无丢包容量、真实客户端体验和模型正确率仍未测，不声称已交付完整 AI 产品。不再扩展其他协议实现。

第二轮：已完成路径假说的反证实验并撤回临时预加载 API；保留 `NodeToMap` 重复遍历修复。同程序、同样本、完整 JSON 的吞吐中位数提升约 12%–22%，10 worker 约 6,579 条/秒、6.80 Mbps 应用输入字节率；全 58,533 条记录导出等价、两协议定向检查及相关 race 检查通过。[实验与原始证据](reports/performance-20260906/OPTIMIZATION_EXPERIMENTS.md) 保留慢值和失败假说，不把局部速度或封装覆盖升级为完整 AI 产品或实时带宽保证。

交付复核：新增 [当前字段证据检查](protocol_corpus_field_inventory_test.go)，把 LDAP/CLDAP、MySQL/MariaDB、PostgreSQL、SMB2/SMB3、Memcached 和 Cassandra 六组原始字段验证与 manifest 双向关联。覆盖 17 份独立捕获、205 条记录，保留负样本与两代数据；同时执行原有逐记录字段断言，LDAP 组另执行 CLDAP 搜索每字段检查。清单守卫验证新增/缺失/重命名/重复、路径/摘要/记录数/分类变化及丢失映射，无法识别的流量仍交由独立分类审计，不能称作自动支持新协议。[字段与边界联合检查](reports/field-audit-20260906/fields-and-boundaries.txt)（2.225 s）及 [race 检查](reports/field-audit-20260906/field-audit-race.txt) 均通过；未运行全库全量。

尚未满足的历史验收项已实际复现：[TestP0RoadmapCovered](reports/field-audit-20260906/p0-state-conflict.txt) 在 LDAP、MySQL、PostgreSQL、SMB3 的目录 `partial` 状态上失败。相应当前样本字段及错误边界复验通过，但路线图 `done`、历史评分卡与 G8 整体状态要求仍不一致。此轮未修改目录状态、P0 门槛、评分卡或协议实现，不将新增样本检查当作 P0 门槛的替代品；整体 goal 不据此宣布完成。

第三轮（2026-09-07）：新增 [有限队列持续回放](reports/replay-20260907/README.md)，按两协议 38 条原始记录循环，执行封装、显式应用字段和完整 JSON 导出。33 轮共 510 秒计划窗口，所有原始计数、丢弃及迟到均保留；2 Mbps 短测无丢弃没有在 30 秒重复中保持，不能给出无损带宽承诺。投递和解析处在同进程，调度抖动与队列影响尚未被独立归因，不把失败直接等同于解析器单独的带宽上限。回放器、六组字段清单与短时实际解析的联合 race 检查通过（5.665 s）。仅增加评估和验证代码，没有新增依赖、客户端/aid 集成、协议实现或改变 14 项延期与历史 P0 门槛。

第四轮（2026-09-07）：保留 [私有小配置存储优化](reports/config-store-20260907/README.md)。1/4/10 worker 各三轮新旧对照，全记录封装/两协议字段吞吐中位数提高约 29%–30%，完整 JSON 提高约 27%–28%；10 worker 约 13.70 Mbps 封装输入、8.71 Mbps 应用字段与 JSON 输入，两类负载不能相加。58,533 条封装记录和 11 条应用消息的字段、类型、元数据、错误与读取位置摘要，各两轮相等；base 全部 race 及公共并发/隔离/复制/缓存/事务检查通过。追加 12 轮、240 秒有限队列对照，6 Mbps / 4 worker 的丢弃率中位数约 20.073% → 0.465%，新版仍有丢弃，2 Mbps 长测也未全轮无损；没有夸大最大带宽或客户端能力。

公共后端变更后已执行一次 `go test ./common/bin-parser/... -count=1 -timeout=5m`，不是整个 Yaklang 仓库全量。主包失败（191.912 s），公共子包通过；日志可见 P0 状态、RMI 分支和评分、WinRM 旧伴随样本、GSS-API 旧合同共五个顶层失败项，均在冻结旧版及新版单独复现。[失败对照](reports/config-store-20260907/failure-comparison.txt) 与 [逐项原因](reports/config-store-20260907/README.md) 已记录。没有改评分卡、测试门槛、目录状态、原始样本或其他协议实现，整体 goal 仍不能宣布完成。下一步验收修复需保持现有严格字段/拒绝合同，不能删负样本或只放宽断言。

第五轮验收对齐（2026-09-07）：[修复与复验记录](reports/acceptance-20260907/README.md) 保留全部失败和限制。修正 RMI 完整消息分支，将旧无效向量保留为负例；原生 YAML-only Schema 不计分，RMI 及评分别名从 95/A 保守下调至 75/B，不增豁免。WinRM 汇总使用既有完整 Identify 正例，同时保留两份历史负例并锁定拒绝合同；GSS-API 评分仍为旧 SPNEGO 子集 80/B，另对齐严格 token corpus 合同。定向测试 2.929 s，通过；bin-parser 包及子包完整复验主包 193.302 s，仅剩 `TestP0RoadmapCovered` 对 LDAP、MySQL、PostgreSQL、SMB3 的四项 `partial` 错误，其余有测试子包通过；定向 race 27.105 s，通过。没有修改协议实现、P0 门槛、目录/路线图、原始捕获或依赖；性能归档重算一致，不重跑计时、不扩大到延期协议或 aid/客户端实现。P0 历史整体要求与本轮样本字段范围的冲突仍需明确验收范围，整体 goal 未标记完成。

第六轮 aid 工具评估（2026-09-07）：新增 [受控离线脚本探针](reports/aid-probe-20260907/README.md)，原样执行现有分析脚本与真实 PCAP 后端，隔离 CLI / 日志 / 文件落盘，不调用模型或完整 aid 回调。8 个用例各三轮，24 次均执行；每轮 5 个用例失败，复现 Cassandra 的 DNS 误计数、离线 BPF 无效、max-packets 越界且回调继续、无效过滤器及坏文件仍报告成功。误计数字节定位到 Cassandra frame 14 包内偏移 51，独立 CQL 字段复验通过。原始失败、源码 SHA 与评估适配边界均归档，不把通过比例当作 AI 正确率，也不用探针耗时估计带宽。仅增加显式标签启用的评估代码和文档，默认包列表不含探针；没有修改协议、aid / PCAP 后端、客户端、依赖、P0 门槛或 14 项延期范围。自动续跑不代表批准降低剩余四项的整体验收要求；整体 goal 仍未宣布完成。
