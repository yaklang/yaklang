# 流解析工具

rule文件以yaml格式编写，支持解析多种格式的数据流，二进制文件、链路层、网络层、应用层协议等。

逐协议交付与打分验收见 [PROTOCOL_DELIVERY.md](PROTOCOL_DELIVERY.md)。P0 打分结果见 [P0_SCORES.md](P0_SCORES.md)，P1 打分结果见 [P1_SCORES.md](P1_SCORES.md)。P0 / P1 标 `done` 前必须过硬门槛并达到 **B 级及以上（总分 ≥ 75）**。A 级（≥ 90）留给 Wireshark 级主 PDU，不是清掉 leftover 之后的默认分。

## 1. rule组成
rule的根节点为Package，其下包含多个子节点，用来描述数据的结构、属性。
如
```yaml
Package:
  Rule1: xxx
  Rule2: xxx
```
## 2. rule节点
每个节点由属性和子节点组成，属性用小写字母开头的key表示，子节点用大写字母开头的key表示。
如一个Package节点
```yaml
Package:
  endian: big
  list: true
  Rule1: xxx
```
其中endian、isList为Package的属性，Rule1为Package的子节点。
## 3. 属性
默认解析器内置了一些属性，包括：
- endian: 字节序，big或little，默认为big
- isList: 是否为列表，true或false，默认为false
- length: 长度，用于描述固定长度的数据，如uint32、ipv4等
## 4. operator
operator是一个特殊的属性，可以编写yak代码，控制解析流程
如
```yaml
Package:
  endian: big
  list: true
  length: 10
  operator: |
    d = this.Rule1.Process()
    if d == 1 {
      this.Rule2.Process()
    }
  Rule1: raw,10
  Rule2: raw,10
```
## 5. Context
operator可以使用context传输上下文数据，如
```yaml
Package:
  endian: big
  list: true
  length: 10
  operator: |
    this.SetCtx("rule1-length", 1)
  Rule1: 
    operator: |
      this.GetCtx("rule1-length")
```
## 6. 默认解析器
可以通过属性、上下文控制默认解析器的解析行为，上面提到了属性，下面介绍上下文
- stopArray: 停止数组解析，true或false，默认为false

## 7. 性能与回归验证

依据 2026-09-06 用户最新指令，14 项外层/识别协议明确延期，不计入本次验收；Cassandra、Memcached 的样本字段与定向验证完成后，立即进入性能、最大带宽和 aid/AI 分析能力评估，不再扩展其他协议。确切名单、顺序和评估口径见 [本次工作范围](ACTIVE_SCOPE.md)。下文 NCP／LAT／SEBEK 阶段的 736 项全量通过、533 份捕获／58,416 条记录及所有基准均是**历史冻结版本**的结果，不能用于宣称当前新增代码已通过全量。当前样本统计、逐协议范围及剩余缺口以 [corpus 清单说明](testdata/protocol-corpus/README.md) 为准；字段级入口也不代表完整会话或所有应用语义已实现。

最新的 Cassandra／Memcached 样本验证及首轮评估见 [PERFORMANCE_ASSESSMENT.md](PERFORMANCE_ASSESSMENT.md)。本机 1/4/8/10 worker 测量、复杂列表、延迟、RSS、CPU/分配热点和 aid/客户端连接情况均有原始记录；离线输入字节率不作为实时最大链路容量。当前 552 份捕获、58,533 条记录的完整性与封装复验通过，但没有重跑整个库的完整测试套件。

内置 YAML 文档只解码一次，每次调用仍创建独立的节点树、配置和上下文，并深拷贝可变文档值。算子与 `out` 表达式共用进程内的有界编译缓存：合计最多 1024 项，单个源码不超过 64 KiB、编译产物不超过 128 KiB；缓存只保留序列化字节，不共享引擎、闭包或运行时变量。包含 `include` 或超出缓存边界的源码继续走普通求值，不改变语言的输入限制。

表达式求值需要暂时移除自己的配置，再读取未格式化的结果。恢复这项临时状态时，若配置对象及赋值历史均未变化，现在只恢复当前值，不再为每次读取保留一个新的赋值闭包。重复 `Result()` 因此不会累积这部分配置历史，也不会让之后的配置重放随读取次数增长。表达式显式修改配置或替换配置对象时，仍保留原来的最后赋值与重放顺序；没有全局压缩 `Config.SetItem` 历史，也没有缓存可变结果树。解析、生成、三个表达式入口、错误恢复和别名/导入回归共同保护这一边界。

默认解析器按逻辑根节点创建运行时，允许不同输入独立并发解析。嵌套导入复用所属根节点的运行时；同一棵可变节点树仍应由调用者串行使用。`Node.Copy` 保留共享上下文语义，不能用作并发隔离；独立输入应分别调用 `ParseBinary` 或创建新的规则树。`RegisterParser` 保留原有共享实例语义，有状态的自定义解析器可使用 `RegisterParserFactory`。注册应在并发处理开始前完成。

先做定向回归与基准，再跑完整套件；性能验证不代替样本完整性、字段值或边界验证。以下命令均在仓库根目录运行：

```sh
go test ./common/bin-parser/parser/stream_parser -run '^$' -bench '^BenchmarkOperatorProgram$' -benchmem -benchtime=500x -count=5
go test ./common/bin-parser/parser -run '^$' -bench '^BenchmarkParseBinaryNTPParallel$' -benchmem -benchtime=1000x -cpu=1,2,4 -count=2
go test ./common/bin-parser/parser/stream_parser -run '^$' -bench '^BenchmarkXMLRPCDeclaration$' -benchmem -benchtime=1000x -count=3 -cpu=1
go test ./common/bin-parser/parser/stream_parser -run '^$' -bench '^BenchmarkNodeResultPosition$' -benchmem -benchtime=100ms -count=5 -cpu=1
go test ./common/bin-parser/parser/stream_parser -run '^$' -bench '^BenchmarkOutProgramSV$' -benchmem -benchtime=100ms -count=3
go test ./common/bin-parser/parser/stream_parser -run '^Test(OutResult|Expression|ExecOutExplicitConfigMutation)' -count=1
go test ./common/bin-parser -run '^$' -bench '^BenchmarkProtocolCorpusSVExecOut(Legacy)?$' -benchmem -benchtime=100x -cpu=1 -count=3 -timeout=90s
go test ./common/bin-parser -run '^$' -bench '^BenchmarkProtocolCorpusSVExecOut(ScalarLegacy)?$' -benchmem -benchtime=100x -cpu=1 -count=3 -timeout=90s
go test ./common/bin-parser -run '^TestProtocolCorpus(IECOut|AARP)' -count=1 -timeout=3m
go test ./common/bin-parser -run '^$' -bench '^BenchmarkDICOMPDVList$' -benchmem -benchtime=1x -count=1 -cpu=1 -timeout=3m
go test -race ./common/bin-parser/parser/... -count=1 -timeout=240s
go test ./common/bin-parser/... -count=1 -timeout=20m
```

2026-09-06 本机 Go 1.22.12、darwin/arm64 测量中，同一算子基准五次中位耗时从每次编译的 94,327 ns 降到缓存后的 27,410 ns；分配次数从 1,220 降到 383。原 499 文件、58,265 记录的同一全量封装测试从 57.224 秒降到 22.968 秒，均遍历全部记录。这些是本机样本结果，不代表所有协议的固定加速比；封装层通过也不代表应用层字段全部实现。优化未增加依赖，当前分析没有显示正则匹配是主要热点。

四协议扩展及空值并发修复后的算子复测（同机、`-cpu=1 -benchtime=500x -count=5`）：每次编译与缓存执行的中位耗时分别为 117,173 ns 和 35,351 ns，约 3.31 倍；分配次数仍分别为 1,220 与 383。只比较这一轮同条件的两组，不跨轮解释绝对耗时差异。

`TestProtocolCorpusSampledValuesEveryASDU` 使用最多 4 个独立 worker（同时受 `GOMAXPROCS` 和测试并行度限制），每个 worker 仍逐帧执行全部断言，结束后再汇总。相同 10,161 帧的本机测试耗时从 218.34 秒降到 110.67 秒；10,161 个 ASDU、650,304 字节采样数据及全部字段值的精确校验未减少。这里的 Sampled Values 是协议名称，并非抽样验证。

XML-RPC 声明是一个单独核验过的正则优化点：复用已有的 `go-pcre2-lite v0.1.6` 原生接口，仅替换这一处预编译表达式，不增加依赖。`DollarEndOnly` 保留原 RE2 的完整结束锚点，文档的 1 MiB、UTF-8 和深度边界不变，匹配引擎错误单独返回。694 组原表达式等价比较、并发匹配/完整解码、精确长度边界和模糊测试覆盖这一替换。

同机 1000 次、三轮暖态正则基准中位数：64 KiB 空白的合法声明从 2.124 ms 降至 31.527 µs，非法声明从 4.728 ms 降至 100.116 µs；常见完整声明从 570.8 ns 降至 354.6 ns，最短声明则从 254.2 ns 变为 259.8 ns。以上只测匹配，不包含 XML 解码或编译，不是全库加速倍数。原生编译对象及并发高水位的匹配临时区按进程生命周期复用；Go 的分配统计不包含 C 内存。

前一性能阶段的完整套件通过，主测试包耗时 220.715 秒，对应当时的 508 文件、58,275 条记录；解析器完整 `-race` 检查与 XML-RPC 表达式等价模糊测试也通过。这一历史测量不代表后续新增测试的耗时，当前样本与覆盖边界以 corpus 清单和验证矩阵为准，不能把外层解析或识别合同当作目标协议完整实现。

协议边界测试中，预期拒绝也是正常结果。Binparser 的独立算子 VM 关闭可选的 Go 调试栈打印，仍逐字保留返回的 Yak 错误、规则源码位置与恢复行为；其他 VM 和公开 `NewVMPanic` 默认不变。这是每 VM 的执行前配置，没有全局日志开关。DICOM 捕获的全部 34,384 个短前缀在本机独立测试中用时 4.058 秒，断言未减少；不与包含大列表资源边界的混合套件作加速比比较。

规则并发回归还覆盖原生回调返回 `nil`、缺失上下文、空错误多值返回与变量别名。VM 对共享的 `undefined` 哨兵创建独立绑定元数据，避免赋值时改写全局值；缓存与非缓存执行均验证，同步、异步及 Lua 空值调用诊断保持一致。不以串行执行代替状态隔离。

DICOM 的 4,096 项正向资源边界继续保留。列表循环改为记录剩余字节后，同一 PDV 上界用例在本机用时 34.348 秒；此前 45.274 秒包含 CPU 采样开销，不能据此宣称固定加速比。剖析仍显示父节点长度计算反复遍历已处理兄弟节点，存在二次扫描；不能直接用当前游标差替代，因为规则支持乱序处理、导入及试探回滚。该残余热点需单独建立等价基准后优化，不能通过删掉大列表测试掩盖。

四协议扩展阶段执行了一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`：主包 361.178 秒，各包合计 348 个顶层测试通过、2 个既有手动导出测试跳过，唯一失败是目录与路线图的一致性检查。全部 58,279 条记录的封装验证通过（23.363 秒）；10,161 帧 SV 的全部字段验证通过（105.60 秒）；DICOM 四个 4,096/4,097 项正反资源边界全部通过。

全量之后修正目录映射：WS-Discovery 使用规范名称并在 Notes 中保留 2005 版本限制；固定路线图之外的 DICOM UL 必须关联真实捕获及一致的规则、入口、层级和范围说明，没有增加无条件跳过。最后的 Powerlink 地址边界修复及目录修复均单独补测。联合目录、路线图、覆盖矩阵、直接解析、WS-Discovery 和 Powerlink 定向测试通过（3.034 秒），Powerlink 竞态测试通过（2.176 秒）；没有再次执行与这两处修复无关的耗时完整套件。以上是全量基线加定向修复证据，不是最终版本单次全绿的全量记录。

解析器和 YakVM 的完整竞态测试、panic/recover/exit 兼容测试通过。解析器 `go vet` 通过；YakVM 的 `go vet` 仍报告三处经 `HEAD` 核对已存在的不可达语句，本次未修改这些无关位置。该四协议阶段的覆盖矩阵区分 395 个直接解析合同、51 个仅识别合同、62 个预期拒绝和 2 个容器结构检查；验证覆盖不等于所有协议变体都已实现。

后续字段读取优化移除 `Result` 对字段之前所有字节的重复读取和分配，只定位并读取字段所需窗口；有待写入的不足一字节数据也只追加到这个窗口。没有增加可变长度缓存。16,668 组有效位区间逐一对照旧算法，覆盖所有位偏移、待写位数、padding、零宽 EOF、原始字节、字符串和大小端整数；终端原始结果仍独立拷贝。畸形手工结果区间现在先返回边界错误，不再允许整数回绕、越界分配或切片 panic。

同机 `-cpu=1 -benchtime=100ms -count=5` 的 16-bit 终端字段微基准中，4 KiB 输入尾部的读取中位数从旧算法的 3,149 ns 降到 245.7 ns，分配从 8,356 B 降到 188 B；这只衡量字段结果读取，不是整个协议的加速比。4,096 项 DICOM 列表的同条件单次完整解析基准，累计分配从 2,117,357,416 B 降到 316,376,832 B（约减少 85%），但耗时分别为 33.23 秒和 35.81 秒，未显示整体 CPU 耗时改善。所有 PDV 和字段仍生成并验证数量；剩余兄弟节点长度扫描仍是待优化项。

C37.118 扩展逐条验证原捕获的 778 条记录，其中 617 条完整协议消息包括 7 条命令、2 条 CFG-2 和 608 条数据；另外 161 条是 TCP 控制记录。配置帧提供站名、通道名、单位、格式和频率定义；数据帧使用调用者按流选择的当前 CFG-2 解码原始数值，缺少配置时显式保留字节并标记 `ConfigurationRequired`。全部 31,330 个短前缀、CRC、嵌套计数、错误配置、混合数值格式和资源边界也验证通过。CFG-3、扩展命令、工程量换算及测量有效性不在本批解码范围内。原样本和 `roadmap_name: null` 保留，当前矩阵为 396 个直接解析合同、50 个仅识别、62 个预期拒绝和 2 个容器结构检查。

字段窗口优化与 C37.118 集成后的冻结代码已完成一次全量 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：361 个顶层测试通过，2 个既有手动导出测试跳过，无失败；主包 381.313 秒。510 个捕获文件、58,279 条记录的封装校验通过（23.216 秒），10,161 帧 SV 的全部 ASDU 和 650,304 字节数据校验通过（109.60 秒），DICOM 四个大列表正反边界全部通过。完整解析器与 YakVM 竞态回归通过；C37.118 的整组公开入口、全部短前缀和字段测试也通过竞态检查（主包 60.668 秒）。这是该阶段的一次完整通过记录，不以仅识别合同冒充字段解析完成。

DICOM 后续优化仅为内置 PDV 列表增加原生线性校验与等形构树，没有为通用节点增加长度缓存。整个有界 body 校验通过后，才一次提交完整的 PDV、长度、上下文、保留位、标志及片段节点；失败使用原有位读取器和输出写入器事务回滚。直接入口与 TCP 导入都启用，继承的显式 `unit: byte` 原样保留；自定义字段定义、非字节单位及普通结构化生成继续执行原 YAML 算子。诊断配置 `dicomPDVLegacy: true` 可显式选择旧路径作为差分参考，不修改正常解析结果或放宽边界。

同一测试程序、同机 darwin/arm64、`-cpu=1 -benchtime=1x -benchmem -count=1` 的完整 PDV 解析比较如下；两侧都包含字段窗口优化，逐次断言完整 PDV 数量，不包含编译耗时。这是单次基准，不是整个库的固定加速比。

| PDV 数量 | 原 YAML 路径 | 原生列表路径 | 原路径累计分配 | 原生路径累计分配 |
| ---: | ---: | ---: | ---: | ---: |
| 128 | 43.48 ms | 2.43 ms | 10.05 MB | 2.21 MB |
| 512 | 375.31 ms | 9.11 ms | 39.58 MB | 8.12 MB |
| 1,024 | 1,580.86 ms | 25.44 ms | 79.01 MB | 16.04 MB |
| 2,048 | 8,098.64 ms | 56.78 ms | 158.13 MB | 31.81 MB |
| 4,096 | 39,750.87 ms | 123.63 ms | 316.74 MB | 63.57 MB |

基准命令：`go test ./common/bin-parser -run '^$' -bench '^BenchmarkDICOMPDVList(Legacy)?/' -benchtime=1x -benchmem -count=1 -cpu=1 -timeout=3m`。其中 4,096 项累计分配从 316,739,968 B 降至 63,565,240 B，约减少 80%；此前字段窗口优化解决的是另一部分重复分配，两次改进不能混为一次比较。关联建立阶段的通用大列表仍走原路径，这个提速不覆盖它们。

新旧路径的 16 组树差分（32 次完整解析）覆盖 1/2/8/128 PDV、独立及 TCP 导入、所有字段值、全局 bit span、节点顺序、Parent/Ctx、单位、空片段形状及保留位；72 次负向检查和两次流边界验证也通过。独立覆盖率检查确认两种入口都实际执行原生提交分支，而非两边均回退到旧路径。桥接测试还检查全部 8 种待写位偏移下的成功、无效尾项、短读、回调错误和完整回滚。真实结构化 `GenerateBinary` 双 PDV 用例验证生成字节及字段树保持一致。定向差分竞态测试通过（39.989 秒），原生校验与桥接竞态测试通过（2.030 秒）。原有 4,096/4,097 项正反边界未删除。

原生 PDV 阶段冻结代码的完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码为 0：374 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包耗时 294.026 秒。全部 58,279 条记录的封装检查通过（23.315 秒），SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据检查通过（107.74 秒）。DICOM 四项资源边界全部通过：4,096 PDV 为 0.10 秒、4,097 PDV 按预期拒绝；关联信息的 4,096/4,097 项仍各需 26.69/26.43 秒，保留为独立待优化热点。完整解析器竞态测试全部通过（stream_parser 12.861 秒），`go vet ./common/bin-parser/parser/...` 通过。此次没有新增依赖，`go.mod`/`go.sum` 未修改；验证矩阵的 396 个直接解析合同、50 个仅识别合同、62 个预期拒绝及 2 个结构检查边界不因性能改进而扩张。

随后将 DICOM 的有界 `User Information` 列表也改为原生线性校验与同形构树：保留最大 PDU 长度、嵌套实施类 UID、实施版本和未知扩展项，仍检查必需项、重复项、字符约束及整个关联的 4,096 项共享上限。成功时才一起提交完整字段树和 Go `int` 类型的 `dicomItemCount`；失败不写入部分列表或计数。显式 `dicomUserLegacy: true` 可选择旧路径，非标准定义/计数/单位及结构化生成仍走旧规则。拒绝边界保持一致，但同时畸形且截断的输入可能改变首个错误的顺序，不承诺底层错误字符串逐字一致。

同机 darwin/arm64、同一程序、`-cpu=1 -benchtime=1x -count=1` 的整个关联 PDU 解析基准中，4,096 项从旧路径的 25,335.41 ms 降到 167.34 ms，累计分配从 383,491,656 B 降到 77,208,904 B（约减少 80%）。基准保留全部 `User Item` 节点，逐次检查实际用户项数及全局条目总数；两侧都包含此前的 PDV 和字段窗口优化，这不是整个库的固定加速比。可复现命令：`go test ./common/bin-parser -run '^$' -bench '^BenchmarkDICOMUserInformation(Legacy)?/' -benchtime=1x -benchmem -count=1 -cpu=1 -timeout=3m`。

该关联列表的 8 组新旧树差分（16 次解析）涵盖 RQ/AC、8/128 个混合扩展项及独立/TCP 导入；已确认两条入口都命中原生提交分支，值、顺序、全局位区间、Parent/Ctx、单位、空扩展项及原始计数类型全部一致。全部小 body 短前缀、必需项/UID/版本负例、TCP 失败后的原样载荷恢复和真实结构化生成也通过。32 组桥接事务覆盖全部 8 种待写位偏移下的成功、无效末项、短读、回调错误及原计数恢复。关联列表差分竞态通过（39.070 秒），两类 DICOM 原生校验/桥接的联合竞态通过（1.992 秒），完整 DICOM 定向测试通过（15.527 秒），没有删除原有 4,096/4,097 项正反资源边界。

DCCP 同阶段补全十种基本包的 13 种合法 type/X 组合、序号/ACK、Service/Reset、常见有界选项及 Data Dropped 位域；使用标准库 CRC32c 验证存在的 Data Checksum 选项，重复选项只计算一次。IPv4/IPv6 导入验证完整或部分覆盖的伪首部校验和，已知路由头采用最终目的地址；未知在途路由和缺失 IP 上下文显式标记需要校验上下文。保留原畸形样本，并新增 13 条可重现的有效记录，均经独立 Wireshark 字段/校验和检查。连接状态、CCID/特性协商、后续扩展和重组不在本阶段范围内，目录仍为 partial。新旧共 40 份 companion 从临时目录重生成后逐字节一致，没有覆盖原捕获。

最终联合 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码 0，399 个顶层测试通过、2 个既有手动导出测试跳过，主包耗时 245.566 秒。511 个文件的全部 58,292 条记录通过封装校验（23.519 秒）；SV 的 10,161 帧、10,161 个 ASDU 及 650,304 字节采样数据全部通过（106.91 秒）。4,096 PDV/关联项分别为 0.12/0.14 秒，两个 4,097 项用例都按预期拒绝，四项资源边界合计 0.26 秒。完整解析器竞态通过（stream_parser 11.998 秒），`go vet ./common/bin-parser/parser/...` 通过。覆盖矩阵是 397 个直接解析合同、49 个仅识别合同、63 个预期拒绝及 2 个结构检查；341 个有材料的路线图名称分为 279 个字段级、24 个外层及 38 个仅识别，不能据此宣称全部协议或变体完成。`go.mod`/`go.sum` 未修改，没有新增依赖。

全量后仅更新文档及生成清单的派生统计，没有改动解析代码或样本：离线运行现有 corpus generator，清单仅有 JSON 排版变化，所有字段语义一致；510 份代表帧 hex 和 11 份许可证副本逐字节不变，511 份捕获的摘要全部匹配。更新后的完整性、原 PR 摘要、目录、覆盖矩阵及清单负向检查通过（主包 1.369 秒），生成器自身测试通过（0.882 秒），PR #5023 重现审计工具测试 3/3 通过；没有为这次纯派生输出刷新重复运行耗时全量。

随后将相同的编译缓存用于 `ExecOut`，每次仍新建引擎并解码独立的代码、符号表和可变字面量，执行时保留表达式的 Inline 模式及最后栈值语义。诊断配置 `outProgramLegacy: true` 选择原求值器，且随显式调用配置传入导入规则。新解码代码递归绑定本次源码，避免动态 `eval` 改写外层错误定位；这不修改通用 YakVM 的字节码格式。13 个表达式回归覆盖完整结果与错误等价、嵌套函数及 defer 源码行、返回闭包、并发隔离、缓存边界和真实 SV 转换；另有三个集成测试检查嵌套 `out` 的解析/结构化生成、逐次结果独立性、字段位区间、失败后配置恢复及 LDAP 到 BER 的导入路径。

同机 darwin/arm64、同一程序、`-cpu=1 -benchtime=100x -count=3` 的正式对照，使用原捕获第一条 120-byte Ethernet/VLAN 帧中的完整 102-byte SV 消息。暖态 SV 入口解析加根节点 `Result` 的中位耗时从 22.732 ms 降至 14.494 ms（减少 36.24%），累计分配从 6,435,948 B 降至 2,956,331 B（减少 54.07%），分配次数从 143,205 降至 54,091。纯解析、追加独立字段结果、单次根结果遍历三个路径的耗时分别减少 33.06%、34.34%、33.10%；所有路径计时前后均验证独立 BER 参考字段和完整输入消耗。这里没有计入 Ethernet 分发链，不是全库固定加速比；三类单表达式的 2.18–3.99 倍提速也不能替代完整消息测量。完整解析器竞态检查通过（stream_parser 13.787 秒），解析器 `go vet` 通过；未增加依赖。

同阶段将原 nDPI eMule/ED2K 捕获从仅识别合同升级为有界 Hello/HelloAnswer 字段解析：全部 17 条记录中的 9 条 TCP 控制记录、2 条 Hello 和 6 条 HelloAnswer 均验证，两个不同完整消息的固定字段、全部标签、能力位、重传序列范围及公开入口位区间都有断言。另有 70 个标签编码组合、256 个原消息短前缀、多消息、资源上限及完整载荷回退测试。两条新增 TCP 端口仅对明确 family marker 使用单候选试探并完整回滚；其他前缀仍保留原 HTTP/TLS 等候选。字符串编码与 FLOAT32 位模式原样保留，未知尾扩展显式为字节；压缩、UDP、其他操作、未实现标签类型、连接状态与重组均不在 partial 范围内。字段布局经原始 eMule 实现独立复核，定向与目录/矩阵检查通过（2.827 秒），整组竞态检查通过（30.080 秒），没有修改原捕获或增加依赖。

该 `out` 缓存及 eMule/ED2K 阶段冻结源码的一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码 0：423 个顶层测试通过、2 个既有手动导出测试跳过，无失败，主包 236.301 秒。511 文件的全部 58,292 条记录通过封装验证（23.344 秒）；SV 的 10,161 帧、10,161 个 ASDU 和 650,304 字节数据均逐项通过（97.11 秒），没有减少字段断言或记录数量。DICOM 的 4,096/4,097 项两类正反边界全部通过，合计 0.23 秒。全量结束后源码摘要与冻结前一致，只更新本说明；没有重复执行全量。该阶段矩阵为 398 个直接解析合同、48 个仅识别、63 个预期拒绝和 2 个结构检查；341 个有材料的路线图名称分为 280 个字段级、24 个外层和 37 个仅识别，未实现的变体仍保持 partial。这是该阶段的整体运行记录，不把与历史套件的时差解释为受控性能基准。

随后修复重复表达式结果读取积累配置赋值闭包的问题，并新增有界 XMPP 字段解析。全部 376 条原始捕获记录和 143 个重组 framing item 均有明确处置；只对真实捕获或调用者显式提供的方向上下文解码核心 stanza，缺上下文的 XML 与未知二进制不计作字段级成功。原始帧、来源及清单保持不变，具体数量、字段和未实现边界见 corpus 说明。表达式回归覆盖不变配置的历史不增长、显式修改后的重放顺序、配置对象替换以及错误恢复，没有用共享结果或跨调用状态换取速度。

这一联合阶段冻结源码后只执行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：438 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 241.372 秒。511 个文件的全部 58,292 条记录通过封装检查（23.268 秒）；SV 的 10,161 帧、10,161 个 ASDU 和全部 650,304 字节数据逐项通过（99.97 秒）；DICOM 四项 4,096/4,097 资源正反边界全部通过，合计 0.23 秒。完整解析器竞态检查通过（stream_parser 14.056 秒），XMPP 全部五组 corpus 测试的竞态检查通过（12.418 秒），解析器 `go vet` 通过。全量结束后 Go、YAML 和 JSON 的摘要与冻结前一致；之后仅更新文档，`go.mod`/`go.sum` 未修改。

该阶段矩阵为 399 个直接解析合同、47 个仅识别、63 个预期拒绝和 2 个结构检查；341 个有材料的路线图名称分为 281 个字段级、24 个外层和 36 个仅识别，没有缺失处置。全量通过证明这些明确合同均被验证，不代表所有协议、操作或变体完整实现，也不把不同阶段的整体耗时差当作受控性能收益。

随后针对 IEC 的三个纯标量 `out` 表达式增加窄范围快路径：只有完整源码逐字节匹配、实际 Go 值类型符合约束时才直接计算。正常解析及未格式化 `Result` 已先执行；不缓存节点、结果、字节切片或引擎，源码编辑与非预期类型仍走原求值路径。`outScalarLegacy: true` 仅关闭这一快路径、保留编译缓存；`outProgramLegacy: true` 同时关闭两项优化。差分检查覆盖确切数值类型、完整结果树、字段跨度、短/长长度、结构化 IECBytes 生成、重复结果隔离、自定义源码副作用和错误恢复。

同机 darwin/arm64、同程序 `-cpu=1 -benchtime=100x -count=3`，完整 102-byte SV 消息解析加根结果的暖态中位耗时从 14.384 ms 降至 3.972 ms（减少 72.39%），累计分配从 2,954,080 B 降至 1,238,653 B（减少 58.07%），分配次数从 54,012 降至 24,986。纯解析、追加独立字段结果、单次根结果遍历的耗时分别减少 70.58%、72.99%、72.31%。每条路径计时前后均检查独立 BER 字段参考与全部输入消耗；两侧都启用编译缓存，这些数字只归因于本轮标量快路径，不包含 Ethernet 分发开销，也不是全库固定提速。

同时补齐 SV/GOOSE 的 Ethernet、802.1Q、单层 802.1ad 和 QinQ 有界解码分发，并新增 AARP 的独立有效 companion。分发成功必须关闭试解析事务；非空失败完整保留原始载荷，空载荷明确拒绝。测试逐条检查 reader 的 EOF 和空事务栈。新增入口仍是 partial 解码合同，不把完整 SV/GOOSE 结构化生成、数据集通道解释或 AARP 地址归属列为已实现。

这一阶段冻结代码只运行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：453 个顶层测试通过、2 个既有手动导出跳过、无失败，主包 168.065 秒。512 份捕获的全部 58,295 条记录通过封装检查（23.447 秒）；SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项通过（20.14 秒）。DICOM 两类 4,096/4,097 项正反资源边界全部通过，合计 0.24 秒。完整解析器竞态通过（stream_parser 14.364 秒），六组 IEC corpus 竞态通过（34.208 秒），五组 AARP corpus 竞态通过（5.276 秒），解析器 `go vet` 通过；生成器单元测试及三项 PR #5023 重现审计工具测试通过。全量后冻结清单的 882 项摘要全部一致，之后仅更新文档；没有新增依赖，`go.mod`/`go.sum` 未修改。

该阶段 512 个合同分为 400 个直接解析、46 个仅识别、64 个预期拒绝和 2 个结构检查；341 个有材料的路线图名称分为 282 个字段级、24 个外层和 35 个仅识别，没有缺失处置。以上完整回归验证这些具体合同，不把封装覆盖或仅识别算作全部应用字段已实现；不同阶段的全套耗时仍不能替代同条件开关基准。

随后新增扩展 DDP、CFM CCM 和 NHRP 七类基本消息的有界字段解码，以及各自的事务式链路分发。十一条独立 companion 记录逐字段、逐位区间验证，三份不完整原始样本保留为负例，此前四十一份 companion 的摘要不变。当前清单为 515 份捕获、58,306 条记录；矩阵为 403 个直接解析、43 个仅识别、67 个预期拒绝和 2 个结构检查，341 个有材料名称分为 285 个字段级、24 个外层和 32 个仅识别。各入口仍保留 partial 与未解释数据的明确边界。

NHRP 的 4,096 条 CIE 资源用例在优化前耗时 127.482 秒；实际 CPU profile 中 `CalcNodeConsumedLength`/`walkNode` 累计占 70.10%，热点是重复扫描已处理兄弟节点，不是正则。新增窄化 CIE 原生桥接仅匹配内置完整原型，先验证全部边界，再在独立树中构建全部原字段及位区间，成功后原子提交。修改过的规则、生成模式和 `nhrpClientsLegacy: true` 保留原 YAML 路径。没有引入共享长度缓存，也没有减少节点、缩小 4,096 条上限或新增依赖；`First Prefix Length` 元数据与旧路径保持确切 Go 类型一致。

同机 darwin/arm64、同程序、单核的暖态受控基准，使用 `-benchtime=3x -count=3` 比较公开 NHRP 入口的解析耗时（包含完整 CIE 节点树，不包含另一次根 `Result()`）：1 条 CIE 的中位数从 1.117 ms 降至 0.874 ms；32 条从 14.866 ms 降至 2.849 ms；128 条从 89.032 ms 降至 12.845 ms，后者为 6.93 倍。128 条的累计分配从 17,769,256 B 降至 7,988,858 B，分配次数从 410,572 降至 181,117。原 4,096 条完整边界用例在优化后通过（0.32 秒），4,097 条按预期拒绝；历史 profile 用例耗时不用于计算上述受控倍率。复现命令：

```sh
go test ./common/bin-parser -run '^$' -bench '^BenchmarkNHRPClientList$/(1|32|128)$' -benchmem -cpu=1 -benchtime=3x -count=3
```

这一阶段冻结后的一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码为 0：481 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 186.760 秒。515 份捕获、58,306 条记录的封装检查通过（23.76 秒），SV 全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项检查通过（21.05 秒）；NHRP 四项客户端／扩展资源边界全部通过（2.71 秒），CFM 的 1,024／1,025 项正反边界通过（3.46 秒）。完整解析器竞态检查通过（stream_parser 15.842 秒），NHRP 定向两包竞态通过（155.773／1.780 秒），CFM 定向竞态通过（75.617 秒），DDP 定向竞态通过（4.329 秒），解析器 `go vet` 通过。全量后冻结清单中的全部 1,489 项文件摘要一致，之后只补录本段说明。生成器及三项 PR #5023 重现工具测试通过；原始样本没有被补充样本覆盖，`go.mod`／`go.sum` 未修改。

接续阶段新增 IGRP、DVMRP v1、XNS IDP 和 TRILL 的有界字段解析及载体分发。四份新 companion 共十条独立记录，原四份有问题的 PR #5023 捕获保留为明确负例，前四十四份 companion 摘要不变。IGRP 的三个路由类别与全部度量字段、DVMRP 的四类 v1 消息及命令、XNS 的地址／Echo／奇数长度垃圾字节校验和，以及 TRILL 六字节头／内层 C-tag／扩展位组都有独立字节和位区间断言。IGRP／DVMRP 的 IPv4 options 先于协议载荷消耗，分片保持完整原始数据。

TRILL 内层导入仅覆盖直接 ICMP／ICMPv6 和 Ethernet/IPv4 ARP，且要求实际消耗等于独立声明的整个内层报文长度；其他 IP 协议、options、分片及隧道保持完整原始数据，防止嵌套导入绕过边界或把未解析载荷错误标成链路尾部。DVMRP 的 Wireshark 交叉检查固定 `-o dvmrp.strict_v3:TRUE`，避免默认启发式把 v1 消息猜成 v3；未通过换成简单样本回避多地址 Request 的差异。完整支持边界见 corpus 说明，四项目录仍为 partial。

本阶段 IGRP 的最大 104 条路由基准使用同机单核 `-benchtime=3x -count=3`，完整公开解析中位 46.029 ms/op，累计分配约 10.752 MB、252,182 次；该数字是基线，不是加速比。短 profile 中重复长度走查仍是热点之一，没有为了正则替换而新增依赖或扩大底层优化。本阶段清单为 519 份捕获、58,316 条记录，矩阵为 407 个直接解析、39 个仅识别、71 个预期拒绝和 2 个结构检查；341 个有材料名称分为 289 个字段级、24 个外层和 28 个仅识别，未实现变体不因全量通过而变成已完成。

这一阶段冻结后仅运行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码为 0：510 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 189.184 秒。519 份捕获、58,316 条记录的封装检查通过（23.312 秒）；SV 全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项通过（22.01 秒）。DICOM／NHRP 既有正反资源边界、IGRP 的 104／105 条路由、DVMRP 的 512 字节、XNS 的 65,536 字节 wire 和 TRILL 的 65,535／65,536 字节边界均通过。完整解析器竞态通过（stream_parser 15.210 秒），新增四类整组竞态通过（IGRP 4.977 秒、DVMRP 4.560 秒、XNS 5.295 秒、TRILL 3.121 秒），解析器 `go vet`、生成器测试及三项 PR #5023 重现工具测试均通过。全量后全部 1,507 个冻结文件摘要一致，随后仅补录本说明；原四十四份 companion 摘要也全部一致。`go.mod`／`go.sum` 未修改，无新增依赖。

接续的 DECnet／TIPC 阶段新增两份 companion、十三条独立记录，原 PR #5023 两份无效载荷均保留为负例，不把原始捕获改写成合法消息。DECnet 覆盖 Phase IV 有计数封装、可选 padding、短／长路由数据头及七类控制格式；TIPC 覆盖 Linux v6.12 的 version 2、payload users 0..3 和类型 0／1／2／3／5／6／7，包括有界扩展头。NSP、TIPC 应用字节、未实现的状态语义与扩展仍保持明确的 partial 边界。当前清单为 521 份捕获、58,329 条记录，矩阵为 409 个直接解析、37 个仅识别、73 个预期拒绝和 2 个结构检查；341 个有材料名称分为 291 个字段级、24 个外层和 26 个仅识别。

这一阶段的性能热点是 IPX 高频载体每次导入大型 `extended_protocols.yaml` 时构建大量无关节点。新增小型 `ipx.yaml` 供 LLC 和 Linux SLL2 使用；旧公开入口保留原字段定义和导入能力，严格的有序定义比较防止两份规则漂移。曾试验将旧入口改成 import alias，但独立回归发现第三方再导入该别名会静默漏解析，因此撤回该方案，不在本次扩大通用嵌套导入语义。原每条记录的 Ethernet 和旧直接入口检查全部保留，并为全部 16,827 条记录追加独立入口的字段检查。

同机 darwin/arm64、Go 1.22.12、单核、暖态 `-benchtime=100x -count=5` 的串行对照，输入为原 Google 捕获第一帧的 50-byte IPX 消息与完整链路帧。两个测试程序仅在 LLC／Linux SLL2 的两条 IPX import 目标上不同，其他规则、代码和基准相同。完整 Ethernet→LLC→IPX 的中位耗时由 1.935764 ms 降至 1.036465 ms（减少 46.46%）；累计分配由 1,117,308 B 降至 517,659 B（减少 53.67%），分配次数从 22,943 降至 10,633。旧直接入口的中位耗时为 1.147718／1.139789 ms，基本不变；最终程序的新独立入口为 0.160235 ms，其约 7.11 倍差异只适用于与旧直接入口的比较，不是整个库的加速倍数。每次计时解析仍检查完整输入消耗和末端字段，完整树值、类型、顺序、全局位区间、Parent／Ctx、生成、失败回滚和并发隔离在七组独立回归中检查。

复现现有三条入口基准：`go test ./common/bin-parser -run '^$' -bench '^BenchmarkProtocolCorpusIPX$' -cpu=1 -benchtime=100x -count=5 -benchmem`。原 `Ctx["path"]` 绝对目录元数据保持不变：移除目录查询的试验收益很小，已撤回；仅修复 `ParseBinaryWithConfig` 对嵌入规则路径使用 OS 分隔符的既有问题，并补冷／暖缓存、工作目录及配置入口回归。没有增加依赖，也没有把热点不在正则的路径机械替换成正则优化。

独立复核还修正了 DECnet long D-ID／S-ID 的 HIORD 必需前缀检查：不符时直接入口明确拒绝，carrier／Ethernet 完整保留原始载荷。恰好只有 6／21 字节路由头的输入仅作为 Routing 层结构接受，标记 `NSP Present=false` 和 `NSP Decoded=false`，不是完整 NSP 消息。最终 DECnet 定向及竞态分别通过（1.949／4.112 秒），TIPC 定向及竞态通过（1.504／9.360 秒），IPX 新旧入口兼容／原载体边界联合定向及竞态通过（0.933／3.672 秒）。完整解析器竞态通过（stream_parser 15.195 秒），最后的路径元数据及配置入口竞态另行通过（parser 2.284、base 3.734、stream_parser 2.498 秒），解析器 `go vet` 通过。

本联合阶段冻结后只运行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：536 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 186.072 秒。521 份捕获的全部 58,329 条记录通过封装验证（23.613 秒）；16,827 条 IPX 记录全部通过 Ethernet、旧直接及新独立入口的完整字段检查（38.32 秒）；SV 的 10,161 帧、10,161 个 ASDU 和全部 650,304 字节数据逐项通过（22.43 秒）。DECnet 的 64／128／244／1,024 项边界、TIPC 的 60 字节头／66,000 字节数据边界及既有 DICOM／NHRP 等正反资源用例均通过。全量后的 1,520 项冻结文件摘要全部一致，原四十八份 companion 摘要也全部一致；之后仅补录本说明。生成器单元测试及三项 PR #5023 重现工具测试通过，`go.mod`／`go.sum` 未修改、无新增依赖。矩阵仍有 24 个仅外层名称和 26 个仅识别名称；全量通过不等于这些目标协议或全部变体已经实现，也不把不同阶段的套件耗时差当作受控优化结果。

随后继续补充 Steam 本地发现、SliMP3、TDMoE。Steam 的原始 48 条记录保持不变：6 条 discovery/status 消息逐字段验证，40 条 TCP 记录和 2 条其他 UDP 记录单独计数；不把整个文件名当作每条消息的协议。有界原生解码覆盖固定到 `fcbb9a107a0ff5293385f24d6c774eeb5e3d4c84` 的 SteamTracking 客户端提取 schema（不是 Valve 发布的标准），支持消息类型 0..16、nested/packed/unpacked 字段以及显式未知字段／group，限制 65,535 字节、共享 4,096 字段／元素和 16 层。加密字节及会话状态不解释，生成入口明确不支持。字段值与原始 bit span 分离；所有字段集合使用列表，保证 `Result` 和 `NodeToMap` 不会覆盖重复字段。

SliMP3 根据 Slim Devices／SlimServer 的十八字节头补充五条独立正例；原五字节 discovery 保留为截断负例。D/d/h/i/a/l/m/2 及 legacy s/r 路径、方向不确定字段、显示码和 1,500 字节接收边界均有定向验证。TDMoE 根据官方 DAHDI 接收代码补充三条独立正例，字段长度遵循 `8 + (signaling ? ceil(channels/4)*2 : 0) + channels*8`，通道数 1..255、每通道八字节；原零样本输入保留为负例。两者的 direct／carrier／UDP 或 Ethernet、原始回退、所有短前缀、资源边界和并发均已验证。此前五十份 companion 的摘要未改变。

上述阶段材料合计 523 份捕获、58,337 条记录；矩阵为 412 个直接解析、34 个仅识别、75 个预期拒绝及 2 个结构检查。341 个材料名称中，294 个有字段级合同、24 个仅外层、23 个仅识别。仍按“定向验证与热点优化 → 代码冻结 → 一次全量”推进，不以这些阶段性数字替代剩余协议的实现。

Steam 最终性能基准使用同一条 184 字节真实 status 消息、同一二进制、预热缓存、单核，每组 100 次迭代并重复五组。只切换 `outScalarLegacy`，完整 `ParseBinaryWithConfig`、`Result` 和原始字节校验的中位耗时为 775,862 → 324,790 ns，约减少 58.1%（2.39 倍）；累计分配中位数为 374,890 → 221,816 B，分配次数 6,450 → 4,121。命令为 `go test ./common/bin-parser -run '^$' -bench '^BenchmarkSteamDiscoveryParseAndResult/' -benchtime=100x -benchmem -count=5 -cpu=1 -timeout=3m`。这是精确标量表达式快路径相对普通表达式执行的受控比较，不代表整库加速。另行移除了原生解码中已知字段的临时未知名称分配，并复用常量 out 表达式；这些改动在上述两组中都已启用，不混入该加速比例。完整字段类型、值、顺序、位置、父节点及导出列表与普通表达式路径逐项一致，无缓存解码值或新增依赖。

本三协议阶段冻结后只运行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：564 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 186.533 秒。523 份捕获的全部 58,337 条记录通过封装检查（23.76 秒）；16,827 条 IPX 记录的三个入口完整字段检查通过（38.31 秒），SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项检查通过（22.10 秒）。三项新协议的全部原始记录、companion、字段与导出列表、失败回滚及资源边界均通过；最终三协议联合竞态检查通过（12.438 秒），解析器 `go vet`、生成器单元测试及三项 PR #5023 重现工具测试通过。全量后全部 1,533 个冻结文件摘要一致，原五十份 companion 摘要也全部一致；之后仅补录本说明及更新顶部阶段摘要，没有再次运行全量。`go.mod`／`go.sum` 未修改，无新增依赖。矩阵仍有 24 个仅外层和 23 个仅识别名称，不能将封装验证或当前字段合同通过表述为所有协议及变体已完成。

随后补充 NAT-T、MACSec 和 WSMP 的三个 companion、共二十二条独立字段向量；此前五十二份 companion 摘要不变。三份 PR #5023 原始输入也保持不变，分别以 marker 后 IKE 长度为零、MACSec 的 SL=0 却不足四十八字节数据、WSMP 不支持的旧版元素 ID 作为明确负例。协议名称、标准引用及来源地址保持准确，不把替换样本当作修复原始证据。

NAT-T 的 UDP 4500 入口区分 keepalive、ESP 和带 marker 的 IKEv1/v2。支持明文头、顺序载荷通用头、Notify／KE／Nonce／Vendor 字段及 SK／SKF 的可见字段；加密内容和未知载荷正文保持原始字节。IKE 长度不包含 marker，v2 nonce 限长、fragment 编号与顺序、v1 尾部对齐均有明确校验，最多 4,096 个载荷。MACSec 支持 TCI/AN/SL/PN 位字段、可选 SCI、数据／ICV／尾部区间；默认十六字节 ICV 是外部 profile 假设，不是线上的长度字段。WSMP 支持 v2 legacy WAVE 元素、v3 subtype 0、变长 PSID、网络与传输扩展及 TPID 0..3，额外 TPID 布局依据 ISO 16460:2021 的 IEEE 1609.3 等价表示。三者都不将字段成功解析等同于应用、算法或会话成功；WSMP PSID 0x20 的 `abc` 仅作为外层布局向量，Wireshark 的内层应用报错没有通过更换 PSID 隐藏。

当前材料为 526 份捕获、58,359 条记录；矩阵为 415 个直接解析、31 个仅识别、78 个预期拒绝、2 个结构检查。341 个材料名称中，297 个有字段级合同、24 个仅外层、20 个仅识别。完整封装覆盖与目标协议语义的实现程度继续分别计数。

三个字节协议均显式拒绝非整字节的声明长度，且在读取输入前失败，不能通过向下取整悄悄丢失尾部位。此限制针对消息长度，不排除原生桥在已有 reader／writer 的非字节起始位置处理完整字节消息；后者的八种起始位偏移另有成功与失败事务检查。

NAT-T 复杂列表的原 YAML 路径反复计算前序节点长度，4,096 项正例与 4,097 项拒绝暴露了分钟级测试耗时。现对精确匹配的内置列表采用两遍线性校验和事务性树构建，先验证整个载荷链再发布字段；自定义规则、非字节单位及生成模式保留旧路径。字段类型、未处理的模板字段、列表顺序、原始 bit span、Origin／Parent／Ctx、元数据及原始字节都与旧路径逐项比较。坏尾部、短物理输入和未对齐 reader／writer 的回滚也单独检查，不以结果数量替代字段正确性。

同一构建、独立预热、单核，完整 `ParseBinaryWithConfig`、`Result` 和原始字节校验，仅切换 `nattPayloadsLegacy`。每组五次迭代、重复五组，中位结果如下。这里只采用包含最终非整字节边界校验的版本；该受控测量期间未并行运行其他 Go 测试或 profile。

| 载荷数 | 原 YAML（ns/op） | 原生列表（ns/op） | 耗时减少 | B/op（原 → 新） | allocs/op（原 → 新） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 947,417 | 697,158 | 26.4% | 617,760 → 470,574 | 11,949 → 9,022 |
| 64 | 32,455,275 | 5,171,125 | 84.1% | 13,090,648 → 3,245,860 | 249,099 → 59,467 |
| 512 | 793,053,658 | 39,033,000 | 95.1% | 101,804,448 → 22,983,147 | 1,937,429 → 417,884 |

命令为 `go test ./common/bin-parser -run '^$' -bench '^BenchmarkNATTParseAndResult$' -benchmem -benchtime=5x -count=5 -cpu=1 -timeout=2m`。512 项约 20.3 倍只适用于上述输入和调用范围，不代表全库或所有 NAT-T 消息的固定加速比。

WSMP 的小报文 profile 显示主要开销在反复进入算子解释器、goroutine ID 查找及程序反序列化，而不是正则。现使用有界原生布局解码和精确匹配的 PSID／length 标量表达式快路径，保留全部字段、未处理别名与列表模板、重复元素顺序、原始位置及结果类型；修改 schema、表达式或节点上下文时回退原路径。额外检查确定性变异输入的新旧接受判定、完整树差分、所有短前缀、资源边界、并发隔离及八种起始位偏移事务。不缓存输入数据或可变结果树，也没有增加依赖。

最终 WSMP 基准使用同一条 26 字节、五个网络扩展和三字节 PSID 的独立输入；同一构建、完整预热、单核、每组 100 次并重复三组。`Legacy` 同时开启 `wsmpLegacy` 与 `outScalarLegacy`，`LegacyParseNativeScalar` 仅开启前者，`Native` 两者均关闭。每次均执行解析、`Result` 及原始字节校验，未把仅原生解码函数的耗时替代公开调用耗时。

| 入口 | 全旧（ns/op） | 旧解析／新标量（ns/op） | 全新（ns/op） | B/op（全旧 → 全新） | allocs/op（全旧 → 全新） |
| --- | ---: | ---: | ---: | ---: | ---: |
| WSMP | 7,722,365 | 3,695,250 | 782,185 | 1,875,329 → 473,784 | 35,160 → 8,493 |
| WSMPCarrier | 9,731,158 | 4,212,834 | 1,129,174 | 2,163,009 → 615,030 | 40,612 → 11,398 |

命令为 `go test ./common/bin-parser -run '^$' -bench '^BenchmarkWSMPParseAndResult$' -benchmem -benchtime=100x -count=3 -cpu=1 -timeout=3m`。两入口的全旧／全新中位耗时比约为 9.87 和 8.62；只适用于上述输入及调用范围，不代表整库的固定加速比。

本阶段首轮冻结全量暴露一处旧测试错误：`TestP1UDPApplications` 把 UDP 500 的二十八字节 IKE 头直接放到 UDP 4500，没有 RFC 3948 要求的四字节 non-ESP marker。该轮为 604 个顶层测试通过、2 个既有手动导出测试跳过、仅这一个测试失败，主包 190.057 秒；全部 1,554 个冻结文件摘要一致。现保留原始错误字节作为严格拒绝及完整 carrier 回退的负例，另增正确 marker 封装的正例和字段断言，不放宽解析器、不修改原始捕获。旧 UDP 用例与三协议全部定向复验通过后，再冻结并重跑全量，因此本阶段不能表述为首轮全量通过或只跑过一次全量。

修正用例后的最终 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码 0：605 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 192.150 秒。526 份捕获的全部 58,359 条记录通过封装验证（24.134 秒）；16,827 条 IPX 记录的三个入口完整字段检查通过（38.79 秒），SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项通过（22.32 秒）。NAT-T 的 4,096／4,097 载荷正反边界通过（0.35 秒），WSMP 的扩展、旧字段和补充长度资源边界通过（0.05 秒），MACSec 的数据／ICV／尾部边界与完整载荷回退也通过。没有删减存量记录、旧字段断言或资源负例。

完整解析器竞态检查通过（parser 2.006、base 4.046、stream_parser 22.298 秒）；新增协议的定向竞态、最终整字节边界竞态及修正后的旧 UDP 用例竞态另行通过。解析器 `go vet`、corpus 生成器单元测试、三项 PR #5023 重现工具测试及清单完整性／合同检查均通过。全量结束后 1,554 项冻结文件摘要全部一致，原五十二份 companion 摘要再次全部匹配；之后只补录本说明，不重复运行全量。`go.mod`／`go.sum` 未修改；XML 声明匹配继续使用库中已有的预编译 `go-pcre2-lite v0.1.6`，没有为本阶段引入新依赖。

全量仍是阶段交付的必要检查，但不在每次局部修改后重复运行：先用定向字段／边界／差分测试定位正确性问题，剖析真实热点并做同构建受控基准，再冻结代码执行完整回归；全量发现问题时修复并重新验证，不能用性能结果替代正确性。当前矩阵仍有 24 个仅外层名称、20 个仅识别名称，已有字段级合同也不代表全部变体、应用正文或会话语义已实现；58,359 条封装检查通过不能表述为全部目标协议语义完成。

后续 FCoE／Fibre Channel／AllJoyn 阶段新增两个 companion、共十条独立记录，之前五十五份 companion 和全部原始捕获保持不变。材料变为 528 份捕获、58,369 条记录；矩阵为 417 个直接解析、28 个仅识别、81 个预期拒绝、2 个结构检查。341 个材料名称中，300 个有字段级合同、24 个仅外层、17 个仅识别。两个原始 FC／FCoE 文件虽然帧字节相同，仍分别保留来源与摘要；仅十八字节的内层 FC 头不足二十四字节，不能借后面的 CRC／EOF 补字段。原 AllJoyn 报文的记录计数为零，却有八字节未引用尾部，按官方接收器的全消费规则保留为负例。

FCoE 的现代十四字节头、内层 FC 区间、IEEE CRC32 和八字节尾部独立验证；CRC 使用 Go 标准库。外层 SOF／EOF 接受集合按 FC-BB-5 Rev 2.00 §7.7 表 22／23 收紧为五种／四种，不混用更宽的通用 FC 枚举或早期草案。裸 FC 支持固定头、单个 VFT 和 Network Header；其他可选布局在 direct 入口显式拒绝、carrier 完整回退，应用正文保留原始字节。已知布局且 END_SEQ 时，EOFt 或带明确需 ACK 类别 SOF 的 EOFn 才提供足够的填充字节分离上下文；上下文不足时不猜测。四字节外层 Ethernet FCS 必须显式指定，只保留、不校验。结构化 `GenerateBinary` 明确不支持，解析树的 `NodeToBytes` 字节保持单独验证。

FCoE companion 是六条支持正例和一条保留负例的混合捕获，不是七条合法消息。第七条 DF_CTL=0x31 声明 legacy Network／Association／16 字节 Device Header，却只有三十二字节 Data Field；其字节及 CRC 保持原样，逐记录验证明确的 unsupported 拒绝与完整回退。七条 CRC 均被 Wireshark 判为 Good，但这不能证明内层字段有效；也没有借助 Wireshark 未报告 expert 错误来掩盖该问题。EOFni 与 EOFa 分别记录“内容标为无效”与“帧已中止”，不合并成一种语义。

AllJoyn 此处仅指 AJNS v0/v1 UDP 名称发现：完整 WHO-HAS／IS-AT 记录、重复有序名称、GUID 字节、地址／端口组合及 transport mask。v0 双地址顺序按官方 codec 的 IPv4 在前、IPv6 在后，不采用某些 Wireshark 版本的反向显示作为字段真值。v1 WHO-HAS 只赋予 U 兼容位语义，其余位以保留字段呈现；不把它们标作有效的 v0 T/S/F。字符串按字节长度保留，不擅自增加 UTF-8 或 GUID 语法校验。1,454 字节是固定版本接收器的资源 profile，不是线上长度字段；不实现消息总线、ARDP、v2 mDNS、服务可用性或会话状态。两个公开入口的结构化生成均显式不支持。

AllJoyn 高计数资源测试的 baseline CPU profile 中，`CalcNodeConsumedLength`／`walkNode` 占 54.62%；其中 92.59% 来自公共 `parseLengthByLengthConfig`，仅 7.41% 来自规则显式调用 `Length`。本阶段只将三个列表循环的显式前序长度扫描改为局部 remaining 计数，并保留 `alljoynNSLegacyLengths=true` 的旧路径，不跳过公共父边界检查。新旧路径逐项比较完整树、未处理模板、字段类型与位置、Origin／Parent、配置／元数据、Result／NodeToMap／原始字节以及失败的实际原因；涵盖 255 个名称、255 个问题加 235 个答案和已有 reader 的八种位偏移。没有声称公共解析器已经线性化。

最终版本的同构建、独立预热、单核基准包含完整 `ParseBinaryWithConfig`、`Result` 及字节校验。普通 184 字节输入使用 `-benchtime=1s -count=3`；1,454 字节高计数输入在首轮只获得每组一次迭代，因此另用固定 `-benchtime=3x -count=3` 重测，以下高计数行仅采用后者的中位数。基准期间没有并行运行其他 Go 测试或 profile。

| 输入／入口 | 原长度扫描（ns/op） | 局部计数（ns/op） | B/op（原 → 新） | allocs/op（原 → 新） |
| --- | ---: | ---: | ---: | ---: |
| 184B／AllJoynNS | 5,661,717 | 5,660,909 | 1,762,164 → 1,752,456 | 34,408 → 34,216 |
| 184B／AllJoynNSCarrier | 7,066,288 | 7,039,838 | 1,991,215 → 1,981,492 | 38,915 → 38,723 |
| 1454B／AllJoynNS | 1,161,125,653 | 1,071,199,361 | 73,622,442 → 72,808,088 | 1,519,579 → 1,503,061 |
| 1454B／AllJoynNSCarrier | 1,193,629,250 | 1,098,773,444 | 75,918,152 → 75,124,861 | 1,567,900 → 1,551,451 |

普通短报文耗时基本持平；高计数两个入口的中位耗时约减少 7.74%／7.95%，样本存在波动，不外推为全库固定加速比。高计数完整调用仍约一秒，公共重复扫描是尚未消除的后续热点。复测命令为 `go test ./common/bin-parser -run '^$' -bench '^BenchmarkAllJoynNSLengthTracking/counts-1454B/' -benchmem -benchtime=3x -count=3 -cpu=1 -timeout=3m`。下一步若优化共享长度计算，必须维护长度范围变更、列表模板、回滚和上下文隔离，不能用删掉边界校验换取速度。

本阶段最终冻结后运行一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json`，退出码 0：633 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 207.017 秒。528 份捕获的全部 58,369 条记录通过封装验证（23.58 秒）；16,827 条 IPX 记录的三个入口完整字段检查通过（38.89 秒），SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项通过（22.34 秒）。AllJoyn 高计数资源检查及完整新旧差分通过（1.85／9.00 秒）；FCoE 的 256 值 SOF／EOF、254 个不支持 DF_CTL、ACK 类填充、八种位偏移及第七条保留负例均通过。未减少旧样本、旧字段断言或资源边界。

最终 AllJoyn 的 11 组定向／竞态检查为 13.252／204.243 秒，FCoE 的 17 组为 2.684／18.325 秒，无竞争报告；前述全部解析器竞态检查另行通过（parser 2.423、base 3.508、stream_parser 21.834 秒）。清单完整性／验证矩阵／名称矩阵／直接解析／实际拒绝五项终验通过（2.063 秒），生成器单元测试通过（1.278 秒），三项 PR #5023 重现工具测试和解析器 `go vet` 通过。最终只读复核确认可选头负例、分隔符范围和字段语义修正已闭合。

全量结束后全部 1,561 项冻结文件摘要一致，原五十五份 companion 摘要全部一致，两个本阶段 companion 的字节也未改；之后仅补录本说明及顶部阶段摘要，没有重复全量。`go.mod`／`go.sum` 未修改；已有预编译 `go-pcre2-lite v0.1.6` 继续用于 XML 声明匹配，本阶段无新增依赖、未提交或推送。当前 300 个字段级名称仍含明确的部分支持边界，24 个仅外层与 17 个仅识别名称尚未完成；这次全量通过不是整个协议目标已经完成。

随后公共消费长度优化只改变实时遍历的常数开销：`BaseKV.LookupItem` 一次返回值与存在性，`CalcNodeConsumedLength` 直接递归求和，避免每个已解析叶子的重复 `Has`／`GetItem` 查表及回调。没有缓存“完成子树”，因为规则、输出表达式和公开 `NodeValue.Origin` 可以修改前序结果或替换同长度 Children，事务恢复也不一定清空字段结果。祖先边界、`length-for-start-field` 的首个名称匹配、目标节点的首个指针匹配、分隔符消费量和原有转换／失败语义均保留；复杂列表的重复前序扫描仍然存在，不能称为线性算法。

同一构建保留 `parseConsumedLengthLegacy=true` 的旧遍历，用于差分及基准；两侧都固定 `alljoynNSLegacyLengths=false`，不混入上一阶段的规则局部优化收益。Go 1.22.12、darwin/arm64、单核、独立预热，完整公开解析、Result、NodeToBytes 和输入消费检查均计时；基准串行运行，没有同时运行其他 Go 测试。以下为五次测量中位数，184B 使用 `-benchtime=1s`，1454B 使用 `-benchtime=3x`：

| 输入／入口 | 旧公共遍历（ns/op） | 直接遍历（ns/op） | B/op（旧 → 新） | allocs/op（旧 → 新） |
| --- | ---: | ---: | ---: | ---: |
| 184B／AllJoynNS | 5,695,535 | 5,645,126 | 1,752,517 → 1,752,483 | 34,216 → 34,216 |
| 184B／AllJoynNSCarrier | 7,099,483 | 7,065,310 | 1,982,382 → 1,982,332 | 38,725 → 38,725 |
| 1454B／AllJoynNS | 1,058,360,681 | 919,942,569 | 72,805,104 → 72,805,496 | 1,503,042 → 1,503,052 |
| 1454B／AllJoynNSCarrier | 1,064,482,542 | 963,531,181 | 75,122,882 → 75,125,968 | 1,551,434 → 1,551,452 |

高计数两入口中位耗时分别减少约 13.08%／9.48%，普通短报文及分配量基本持平；这不是所有协议的固定收益，也不是与上一阶段百分比相加后的收益。复测使用 `go test ./common/bin-parser -run '^$' -bench '^BenchmarkParserConsumedLength/counts-1454B/' -benchmem -benchtime=3x -count=5 -cpu=1 -timeout=4m`。独立树基准 `BenchmarkConsumedLengthTree` 仅测遍历，不含公开解析，不能拿它的比例替代这里的端到端数据。

新增差分检查包含 1,024 个确定性随机树的八种状态、宽 4,096／深 512 的树、结果存在但值为 nil、无效消费量、数值转换及原有整数回绕；规则检查覆盖八种位偏移、动态父边界和长度缓存配置、重复／乱序解析、前序 Config 删除及 Children 替换、内层提交与外层恢复。公开入口对照另外检查 UDP 导入后的实际上下文开关、完整字段树、配置／元数据、Parent／Origin、结果类型与原始字节。上述异常配置用例用来保持兼容性，不将历史整数回绕表述为新的合法报文规则。

本阶段冻结后一次完整 `go test ./common/bin-parser/... -count=1 -timeout=20m -json` 退出码 0：646 个顶层测试通过、2 个既有手动导出测试跳过、无失败，主包 202.576 秒。528 份捕获的全部 58,369 条记录通过封装检查（23.70 秒）；16,827 条 IPX 记录的三个入口完整字段检查通过（38.60 秒），SV 的全部 10,161 帧、10,161 个 ASDU 和 650,304 字节数据逐项通过（21.18 秒）。AllJoyn 高计数资源与规则新旧完整差分通过（1.62／8.52 秒）。新增随机树差分和公共入口差分在这次全量中分别为 0.02／0.11 秒，没有通过减少样本或字段检查换取速度。

完整解析器竞态检查通过（parser 2.219、base 4.231、stream_parser 21.556 秒），公共入口差分竞态通过（2.609 秒），解析器 `go vet` 通过。全量结束后全部 1,566 个冻结文件摘要及原五十五份 companion 摘要一致；本阶段未新增或改写任何捕获、规则、清单或已有字段合同。随后仅补录说明及顶部摘要、修正一处基准注释措辞，没有改变可执行代码。`go.mod`／`go.sum` 未修改，继续使用已有预编译 `go-pcre2-lite`，无新增依赖、未提交或推送。24 个仅外层与 17 个仅识别名称仍待实现；全部记录的封装检查通过不代表其应用语义已完成。

### GIOP 与发现报文阶段

在上述已合并分支上，GIOP 1.0–1.3 增加原生 CDR 字段树、明确消息边界和有界连续消息入口；MIOP 增加显式包头入口及单包内 GIOP 字段。WPAD 与 Redfish-SSDP 采用独立的请求／发现报文合同，不再把普通 HTTP 头当作目标协议的字段证据。新增的两个 companion 共 30 条记录单独存放，原 57 份 companion 及上游捕获不变。当前共 530 份捕获、58,399 条记录；材料名称矩阵为 303 个字段级、21 个仅外层、17 个仅识别。详细原始输入判定、来源及不支持范围见 [corpus 说明](testdata/protocol-corpus/README.md)。

这些入口只解析本地字节：不推断代理使用、不解析或执行 PAC、不进行发现或 REST 请求，不自动重组 TCP 或 GIOP/MIOP 分片。IDL 参数及 Profile/Context 内部内容保留为字节。原始历史 ZIOP 的有界解压只在独立测试中用于记录证据，生产入口不支持 ZIOP。GIOP carrier 对 1–7 个非整字节残位前置拒绝，不能以向下取整的 raw 回退掩盖未消费位；直接与八种全局位偏移导入均检查读写状态和事务栈。

P1 评分的 Schema 扫描仍只认识 YAML 声明，因此 IIOP/GIOP 与 IIOP Locate 的原生字段暂不计 Schema 分，明确保留为 75/B。字段、位区间、保留字节和错误边界另有可执行测试；没有为原生实现增加评分豁免。这一评分限制与协议实现边界分别记录，不能用分数代替完整语义验证。

历史 P0 `WPAD proxy` 的 95 分仍只属于其路线图限定的 `HTTP GET /wpad.dat` 封装，不转授完整 WPAD。专用目录必须保留 `partial`；旧“P0 目录不得 partial”的状态检查在这一个历史范围上改为无条件执行实际行为合同，锁定限定范围、评分来源、独立入口、未裁剪输入和未实现语义的元数据。其他 P0 状态门槛不变，将该目录改成 `new` 或退回通用 HTTP 均不能通过。

本阶段另一个可测热点在测试辅助逻辑：评分检查和文档生成每轮为 144 张卡反复解析同一批 Go 测试源码。现在单次测试进程只建立一次静态源码索引，保留原来的重复 Run 名称、字符串匹配、失败文件排除规则和别名规则；返回的名称 map 是副本。当前所有评分卡／路线图规则逐项与独立旧 AST 扫描比对，小型源码 fixture 另保留逐规则旧扫描对照，并覆盖非字面量、重复调用、大小写、损坏源码、嵌套目录及并发读者隔离。规则判断和评分门槛未改变，没有删减协议或样本断言。

同机 Go 1.22.12、darwin/arm64，串行独占 `-cpu=1 -benchtime=1x -count=3 -benchmem` 的最终源码同一二进制基准中，完整 144 卡的源码证据计算中位耗时从旧重复扫描的 **9.512010417 秒**降到**69.896709 毫秒**。新路径把冷索引构建和返回 map 副本都算入，不只测预热后的查表。复测命令：`go test ./common/bin-parser -run '^$' -bench '^BenchmarkP1SourceEvidenceFullCards$' -cpu=1 -benchtime=1x -count=3 -benchmem`。这是评分辅助测试的开销优化，不是协议解码器或整套测试的同等倍数加速；测试期间应保持被审计源码不变。

本阶段首次全量唯一失败为历史 P0 状态门槛与 WPAD 真实支持范围的冲突；其余 701 项顶层测试通过。补齐上面的限定范围行为审核后，定向 P0/P1/WPAD 回归通过（3.904 秒），范围／源码索引竞态回归通过（3.470 秒）。随后重新冻结全部 1,588 项文件，最终执行 `go test ./common/bin-parser/... ./common/bin-parser/testdata/protocol-corpus/tools/generate -count=1 -timeout=20m -json`，退出码 0：**702 项顶层测试通过、2 个既有手动导出项跳过、无失败**，主包 **194.809 秒**。

最终全量逐项验证 530 份捕获的 58,399 条记录（封装检查 24.12 秒），16,827 条 IPX 记录的完整字段（38.37 秒），以及全部 10,161 个 SV ASDU（21.59 秒）。捕获矩阵为 417 个直接解析、28 个识别合同、83 个预期拒绝和 2 个结构容器；名称矩阵仍为 303 个字段级、21 个仅外层和 17 个仅识别，字段级也保留各自的部分实现边界。原始输入不变；新增 GIOP 和 Redfish-SSDP companion 的全部 30 条记录通过相同清单及独立字段检查。

GIOP／URL、新 WPAD／MIOP、Redfish-SSDP、非整字节 carrier 及静态索引的定向竞态检查分别通过；解析器 `go vet`、VM 隔离相关回归和三项 PR #5023 重现工具测试通过。全量结束后全部 1,588 项冻结摘要、57 份旧 companion 摘要一致，`go.mod`／`go.sum` 与合并状态无差异。之后只补录本 README 的最终结果，没有改动可执行代码、样本或其他合同，没有再跑一次全量。已有 `go-pcre2-lite` 继续使用，未增加依赖，未提交或推送。

### NCP、LAT、SEBEK 与配置查表阶段

继续在已合并 PR #5022 和 #5023 的协议优化分支上增加三个限定范围的字段入口。NCP 支持明确的连接控制、部分服务请求和未关联响应头；LAT 支持 Run、Start 5.1、Stop 及所列 slot 布局；SEBEK 支持 v2/v3 记录头与 v3 type 2 端点布局。NCP 经 UDP 524、LAT 经 Ethernet 0x6004 导入，SEBEK 只提供显式入口，不凭可配置端口猜测。未实现的正文语义和会话状态明确标记，不把完整字节保留当作应用语义解码。详细字段、标准来源和限制见 [corpus 说明](testdata/protocol-corpus/README.md)。

原 NCP 和 LAT 短报文不符合其声明的结构，保留原字节、摘要及真实拒绝原因；原 SEBEK 记录逐字段验证。新增三个 companion 共 17 条记录，原 59 份 companion 未改写；材料变为 533 份捕获、58,416 条记录。LAT companion 中 Wireshark 对缺少服务类上下文的正文显示及保留位显示存在已记录的差异，不以无 expert 提示代替字段真值。新旧记录逐条检查字段类型、位区间、原字节和 carrier 回退，并覆盖短前缀、资源上限、导入偏移、事务恢复及独立解析状态。

性能修改仅将 `Config.SetItem`、`AppendConfig` 和 `NewConfig` 的重复存在性／取值查表合并为一次 `LookupItem`；没有缓存可变配置或 cwd。独立旧实现对照保留 nil 与缺失的区别、浅共享、删除后的历史重放、选项顺序及原有错误行为。同机 Go 1.22.12、darwin/arm64、独占 `-cpu=1 -benchtime=500ms -count=5 -benchmem` 的同一二进制微基准中，继承／写入／合并中位耗时为旧路径 4,090 ns、新路径 3,836 ns，约减少 6.21%；两侧均为 3,024 B/op、60 allocs/op。这只是配置操作，不是全库加速比。复测命令：`go test ./common/bin-parser/parser/base -run '^$' -bench '^BenchmarkConfigInheritanceAndWrites$' -cpu=1 -benchtime=500ms -count=5 -benchmem`。

本阶段起点与最终冻结源码分别构建测试二进制，串行独占运行同一 IPX 基准；两侧均无 CPU profile，使用 `-test.run '^$' -test.bench '^BenchmarkProtocolCorpusIPX$' -test.cpu 1 -test.benchtime=500ms -test.count=3 -test.benchmem`，工作目录为 `common/bin-parser`。下表是三轮中位数；最终二进制还包含本阶段新增协议与分派，不能把全部差值单独归因于配置查表。

| IPX 入口 | 阶段起点（ns/op） | 最终版本（ns/op） | B/op（起点 → 最终） | allocs/op（起点 → 最终） |
| --- | ---: | ---: | ---: | ---: |
| direct | 1,171,279 | 1,111,492 | 693,051 → 693,092 | 14,480 → 14,481 |
| standalone | 161,865 | 158,140 | 95,677 → 95,719 | 2,175 → 2,176 |
| ethernet | 1,077,503 | 1,051,612 | 533,555 → 537,312 | 10,938 → 11,015 |

三入口中位耗时分别减少约 5.10%、2.30%、2.40%，分配量没有改善，Ethernet 分派的分配略增；不外推其他协议。路径元数据计算仍是后续候选热点，直接缓存 cwd 会改变运行中切换目录的可观察行为，本阶段未修改这一兼容边界。已有预编译 `go-pcre2-lite` 继续使用，未增加依赖，也未机械替换其他正则。

最终冻结后执行一次 `go test ./common/bin-parser/... ./common/bin-parser/testdata/protocol-corpus/tools/generate -count=1 -timeout=20m -json`，退出码 0：736 项顶层测试通过，`TestP1DumpInventory` 和 `TestDumpP1CeilingCaps` 两个既有手动导出项跳过，无失败；主包 192.543 秒。58,416 条记录的封装检查用时 22.74 秒，16,827 条 IPX 记录的完整字段检查用时 37.82 秒，全部 10,161 个 SV ASDU 用时 21.86 秒。矩阵为 421 个直接解析合同、25 个识别合同、85 个预期拒绝、2 个结构容器；341 个名称中 306 个有字段级合同、21 个仅外层、14 个仅识别，字段级合同也有各自的部分实现边界。

NCP、LAT、SEBEK 和配置兼容性定向竞态检查通过；解析器 `go vet` 和三项 PR #5023 重现工具测试通过。全量前后全部 1,604 项冻结摘要及原 59 份 companion 摘要一致；之后仅补录本 README 的结果，没有再改可执行代码、样本或合同，也没有重复全量。`go.mod`／`go.sum` 未修改，未提交或推送。本次全量完成了现有材料与明确合同的验证，不代表剩余协议或全部应用语义已实现。

### 完整解析性能与 P0 消息范围验收（2026-09-07）

本轮保留原公开调用链和完整输出，相对 `5b6cbe1f8` 测量；可执行 P0 范围、四项 `partial` 的能力边界和评分依据见 [P0_SCOPE.md](P0_SCOPE.md)。源码/程序冻结、正式窗口、全包与 race 验证、环境差异及撤回实验见 [本轮报告](reports/hotpath-20260907/README.md)。当前结果只描述离线解析，不能换算为真实持续无丢包带宽。
