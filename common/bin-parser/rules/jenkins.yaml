Package:
  Jenkins:
    operator: |
      this.ProcessSubNode("Prefix")
      p = string(getNodeResult("Prefix").Value)
      if p != "Protocol:" {
        panic("jenkins: expected Protocol key")
      }
      this.ProcessSubNode("Value")
    Prefix: string,9
    Value: "del:\n;type:string"
    Session: "del:\n;type:string"
