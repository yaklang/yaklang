# Explicit complete LDAPv3 BindRequest (restricted BER), RFC 4511.
# Long-form definite lengths are accepted without imposing DER minimality.
# Simple/SASL octets and optional control values are retained, not evaluated.
# DN grammar, mechanisms, controls, sessions and transport are not validated.
# Unknown operation/choice/extensions and invalid envelopes use raw fallback.
# Bounds: 1 MiB input, four BER length octets and 4096 controls.
endian: big
unit: byte
Package:
  LDAPBindRequestFields:
    operator: |
      err = parseLDAPFields("bind-request")
      if err != nil { panic(err) }
  LDAPBindRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPBindRequestFieldsExact", "LDAPBindRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPBindResponseFields:
    operator: |
      err = parseLDAPFields("bind-response")
      if err != nil { panic(err) }
  LDAPBindResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPBindResponseFieldsExact", "LDAPBindResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPUnbindRequestFields:
    operator: |
      err = parseLDAPFields("unbind-request")
      if err != nil { panic(err) }
  LDAPUnbindRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPUnbindRequestFieldsExact", "LDAPUnbindRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPSearchRequestFields:
    operator: |
      err = parseLDAPFields("search-request")
      if err != nil { panic(err) }
  LDAPSearchRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPSearchRequestFieldsExact", "LDAPSearchRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPSearchEntryFields:
    operator: |
      err = parseLDAPFields("search-entry")
      if err != nil { panic(err) }
  LDAPSearchEntryFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPSearchEntryFieldsExact", "LDAPSearchEntryFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPSearchDoneFields:
    operator: |
      err = parseLDAPFields("search-done")
      if err != nil { panic(err) }
  LDAPSearchDoneFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPSearchDoneFieldsExact", "LDAPSearchDoneFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPSearchReferenceFields:
    operator: |
      err = parseLDAPFields("search-reference")
      if err != nil { panic(err) }
  LDAPSearchReferenceFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPSearchReferenceFieldsExact", "LDAPSearchReferenceFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPModifyRequestFields:
    operator: |
      err = parseLDAPFields("modify-request")
      if err != nil { panic(err) }
  LDAPModifyRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPModifyRequestFieldsExact", "LDAPModifyRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPModifyResponseFields:
    operator: |
      err = parseLDAPFields("modify-response")
      if err != nil { panic(err) }
  LDAPModifyResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPModifyResponseFieldsExact", "LDAPModifyResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPAddRequestFields:
    operator: |
      err = parseLDAPFields("add-request")
      if err != nil { panic(err) }
  LDAPAddRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPAddRequestFieldsExact", "LDAPAddRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPAddResponseFields:
    operator: |
      err = parseLDAPFields("add-response")
      if err != nil { panic(err) }
  LDAPAddResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPAddResponseFieldsExact", "LDAPAddResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPDelRequestFields:
    operator: |
      err = parseLDAPFields("del-request")
      if err != nil { panic(err) }
  LDAPDelRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPDelRequestFieldsExact", "LDAPDelRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPDelResponseFields:
    operator: |
      err = parseLDAPFields("del-response")
      if err != nil { panic(err) }
  LDAPDelResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPDelResponseFieldsExact", "LDAPDelResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPModifyDNRequestFields:
    operator: |
      err = parseLDAPFields("modifydn-request")
      if err != nil { panic(err) }
  LDAPModifyDNRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPModifyDNRequestFieldsExact", "LDAPModifyDNRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPModifyDNResponseFields:
    operator: |
      err = parseLDAPFields("modifydn-response")
      if err != nil { panic(err) }
  LDAPModifyDNResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPModifyDNResponseFieldsExact", "LDAPModifyDNResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPCompareRequestFields:
    operator: |
      err = parseLDAPFields("compare-request")
      if err != nil { panic(err) }
  LDAPCompareRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPCompareRequestFieldsExact", "LDAPCompareRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPCompareResponseFields:
    operator: |
      err = parseLDAPFields("compare-response")
      if err != nil { panic(err) }
  LDAPCompareResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPCompareResponseFieldsExact", "LDAPCompareResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPAbandonRequestFields:
    operator: |
      err = parseLDAPFields("abandon-request")
      if err != nil { panic(err) }
  LDAPAbandonRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPAbandonRequestFieldsExact", "LDAPAbandonRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPExtendedRequestFields:
    operator: |
      err = parseLDAPFields("extended-request")
      if err != nil { panic(err) }
  LDAPExtendedRequestFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPExtendedRequestFieldsExact", "LDAPExtendedRequestFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
  LDAPExtendedResponseFields:
    operator: |
      err = parseLDAPFields("extended-response")
      if err != nil { panic(err) }
  LDAPExtendedResponseFieldsCarrier:
    operator: |
      if !this.HasMaxLength() || this.GetMaxLength("bit") == 0 || this.GetMaxLength("bit") % 8 != 0 { panic("ldap-fields: explicit nonempty byte boundary required") }
      if this.GetMaxLength() > 1048576 { panic("ldap-fields: exceeds 1 MiB resource limit") }
      candidate, trial = this.TryProcessByType("LDAPExtendedResponseFieldsExact", "LDAPExtendedResponseFields")
      if trial.OK { trial.Save(); return }
      trial.Recovery()
      raw = this.NewUnknownNode("Unparsed LDAP Wire")
      raw.SetMaxLength(this.GetMaxLength())
      raw.Process()
LDAPBindRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPBindRequestFields"
LDAPBindResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPBindResponseFields"
LDAPUnbindRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPUnbindRequestFields"
LDAPSearchRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPSearchRequestFields"
LDAPSearchEntryFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPSearchEntryFields"
LDAPSearchDoneFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPSearchDoneFields"
LDAPSearchReferenceFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPSearchReferenceFields"
LDAPModifyRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPModifyRequestFields"
LDAPModifyResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPModifyResponseFields"
LDAPAddRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPAddRequestFields"
LDAPAddResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPAddResponseFields"
LDAPDelRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPDelRequestFields"
LDAPDelResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPDelResponseFields"
LDAPModifyDNRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPModifyDNRequestFields"
LDAPModifyDNResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPModifyDNResponseFields"
LDAPCompareRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPCompareRequestFields"
LDAPCompareResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPCompareResponseFields"
LDAPAbandonRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPAbandonRequestFields"
LDAPExtendedRequestFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPExtendedRequestFields"
LDAPExtendedResponseFieldsExact: "import:application-layer/ldap_fields.yaml;node:LDAPExtendedResponseFields"
