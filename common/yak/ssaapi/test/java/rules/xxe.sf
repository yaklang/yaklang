desc(title: "xxe")

DocumentBuilderFactory.newInstance() as $dbf
$dbf?{!setFeature()}  as $unsafeDBF
$unsafeDBF.newDocumentBuilder.parse(* #-> ?{opcode: param} as $target)


desc("xxe") 

DocumentBuilderFactory.newInstance().newDocumentBuilder().parse(* as $stream)
$stream #-> * as $target

$target?{have: "args"} 
check $target then "fine" else "xxe SyntaxFlow error"

check $target?{have: "args"} then "fine" else "xxe SyntaxFlow error"
