ut = brute.New(
    "telnet", brute.okToStop(true),
    brute.autoDict()
)~

for result in ut.Start([

]...)~ {
    result.Show()
    if result.Ok {
        exec.System(`bash -c "` + f"echo ${result.String()} >> /tmp/telnet-weak.txt" + `"`)
    }
}
