# PrestaShop Deferred Large Fixtures

- `classes/controller/AdminController.php`
  - Current status: AST is correct, but with the current CMS-compatible grammar it remains slightly above the 30s syntax budget.
  - Why deferred: narrowing CMS `expr::member` / `app(...)[...] = ...` support kept `PrestaShop` stable overall, but this one file still hovers around 32s.
  - Follow-up: profile the remaining `phpBlock` / top-level `use` ambiguity and class-constant initializer hotspots without widening CMS support again.
- `src/PrestaShopBundle/Install/Install.php`
  - Current status: AST is correct, but serialized project rerun still isolates it at about `2m02s`.
  - Why deferred: this is far beyond the normal `30s` line and needs dedicated profiling rather than blocking the corpus sweep.
- `tests/Integration/Behaviour/Features/Context/CommonFeatureContext.php`
  - Current status: AST is correct, but serialized project rerun still isolates it at about `53.52s`.
  - Why deferred: this integration fixture is now a durable large-file hotspot and should stay out of the normal project budget path until profiled.
- `tests/Integration/Behaviour/Features/Context/Domain/CartFeatureContext.php`
  - Current status: AST is correct, but serialized project rerun still isolates it at about `1m20.76s`.
  - Why deferred: this file is another true large hotspot and should not block the current 10-project sweep.
- `tests/Integration/Behaviour/Features/Context/Domain/Discount/DiscountFeatureContext.php`
  - Current status: AST is correct, but serialized project rerun still isolates it at about `1m20.07s`.
  - Why deferred: this context fixture remains far above the normal `30s` budget and belongs in the deferred-large lane for now.
- `tests/Integration/Behaviour/Features/Context/Domain/OrderFeatureContext.php`
  - Current status: AST is correct, but serialized project rerun still isolates it at about `3m16.64s`.
  - Why deferred: this is now the worst remaining PrestaShop hotspot and must be treated as a dedicated profiling target instead of blocking the main corpus sweep.
